# Story 4.6 — Capstone (a experiência): Diploma vivo (telemetria real do aluno) + diagrama draw.io de topologia/segurança

> **Epic:** [EPIC-004 — "Nível Produção"](../epics/EPIC-004-nivel-producao.md)
> **Status:** Ready
> **Created:** 2026-07-01 by @sm (River)
> **Sprint context:** Story do **ponto de vista do aluno** (o elemento memorável de fechamento da Grande Final, plano §1) — construída pelo squad (`@dev`/`@ux-design-expert`/`@analyst`). É a **última** story do épico: renderiza/documenta o **estado final** — o Diploma consome a telemetria real (EPIC-003 3.4/F6), o draw.io documenta a topologia que as Stories 4.1/4.2 construíram.
> **Depends on:** Story 4.5 (o Diploma é "surfaced" — linkado/impresso — no checkpoint do Template Repo) · EPIC-003 Story 3.4 (F6 Flow Visualizer — fonte de telemetria real, `AppInsightsFlowEventRepository`) · Stories 4.1/4.2 (a topologia que o draw.io documenta)
> **Blocks:** nenhuma — é a story final do épico (fecha o **SC-7** e contribui ao **DoD** do EPIC-004)
> **Source (Art. IV — rastreabilidade):** EPIC-004 §Escopo Frente C ("Diploma vivo... Diagrama draw.io") + tabela de Stories (linha S6) + §Decisões pendentes ("Onde o Diploma vivo é servido... @sm/@dev + @ux design") · plano `giggly-mapping-hearth.md` §1 (A Visão) · [ADE-009](../architecture/ade-009-network-secrets-service-identity.md) (topologia a documentar)
>
> **Nota de numeração (@sm AUTO-DECISION):** Sexta e última story da série 4.x.

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@pm"
quality_gate_tools:
  - "ux-review"
  - "experience-validation"
architect_gate: "@architect"        # fidelidade do draw.io à ADE-009
ux_support: "@ux-design-expert"      # design "viral" do Diploma
analyst_support: "@analyst"          # draw.io
```

**Rationale:** `@dev` (Dex) implementa a rota/página React do Diploma e a consulta de telemetria real. `@ux-design-expert` (Uma) desenha a experiência "viral" (o elemento compartilhável, LinkedIn-style). `@analyst` (Atlas) produz o draw.io (mesmo padrão validado nas Oitavas/Quartas). Dois gates: `@pm` (Morgan) valida a **experiência** (é o elemento memorável da aula — decisão de produto, não só técnica); `@architect` (Aria) valida a **fidelidade** do diagrama à ADE-009 (nenhum componente/ícone inventado ou desatualizado).

---

## Story

**Como** aluno da Grande Final (que completou o **ato Blindar** — grants + a demo-dinheiro do bypass — e viu sua compra atravessar os 5 microsserviços no F6),
**eu quero** receber um **Diploma vivo** que renderiza a telemetria **real** da minha própria sessão (meus correlation-IDs, a região onde meu sistema rodou, a hora exata do meu deploy) — assinado *"Squad AIOX + você"* — e enxergar num diagrama a topologia de segurança completa que eu mesmo blindei (VNet, Private Endpoint, Managed Identity, Key Vault, `X-Gateway-Key`),
**para que** eu tenha uma prova **infalsificável e pessoal** ("a Copa que você construiu, agora te responde") de que construí um sistema de nível produção — algo que quero compartilhar, não só um certificado genérico de conclusão.

---

## Acceptance Criteria

> **Rastreabilidade (Art. IV):** cada AC cita a fonte do plano/épico ou o artefato real reusado. Nenhum AC foi inventado.

### Bloco A — Localização e branch

1. **AC-1 — Diploma vivo é uma **rota nova no frontend React já existente** (`Lovable/World Cup Tickets Hub/`), não uma página estática isolada no Template Repository.** `[AUTO-DECISION]` — ver §"Decisão de design" abaixo. Código React vive na branch `phase-08-hardening` (mesma branch das Stories 4.1-4.4, per tabela de Stories do épico). O draw.io vive em `docs/diagrams/` (pasta nova) neste repositório. A Story 4.5 (`lab-grande-final.yml`) **imprime a URL do Diploma** no checkpoint final do workflow (mesmo padrão de "imprime URLs" já estabelecido) — é assim que ele é "surfaced" no Template Repo, sem duplicar código lá.
   *(Fonte: EPIC-004 tabela de Stories linha S6 — "`phase-08-hardening` (Diploma React) + `docs/diagrams/` (draw.io) + surface no Template Repo".)*

### Bloco B — Diploma vivo: telemetria real, infalsificável

2. **AC-2 — Fonte de telemetria: reusa o mecanismo já provado do F6 (EPIC-003 Story 3.4), não inventa uma nova consulta.** `AppInsightsFlowEventRepository` (verificado em `src/Fifa2026.V2.FlowEvents/Data/`) já consulta o App Insights/Log Analytics via `Azure.Monitor.Query` + `DefaultAzureCredential` (o mesmo padrão MI que a Story 4.1 generaliza) para alimentar o F6. O Diploma reusa **essa mesma fonte** (não uma tabela SQL separada, não uma API nova de telemetria) — uma nova consulta agregada (correlation-IDs recentes da sessão do aluno + timestamp) sobre os traces que o F6 já indexa.
   *(Fonte: verificado `Azure.Identity`/`Azure.Monitor.Query` em `Fifa2026.V2.FlowEvents.csproj`; ADE-009 §Verificação "padrão MI já existe" (farol reusado por 4.1); plano §1 — "renderizado da telemetria real do aluno".)*

3. **AC-3 — Dados renderizados: correlation-IDs (das compras reais do aluno na sessão), região de deploy, hora do deploy.** Região e hora de deploy vêm de **metadados de build/ambiente já disponíveis** (ex.: `WEBSITE_SITE_NAME`/região do App Service, timestamp injetado no build do workflow — sem inventar uma nova fonte de verdade) — não são "encenados" no frontend (client-side), são resolvidos no backend/build e servidos como dado, tornando-os **infalsificáveis** pelo navegador do aluno (a mesma lógica de "correlationId gerado no backend, nunca no cliente" já estabelecida no fluxo de compra, ADE-000 Inv 5).
   *(Fonte: plano §1 — "correlation-IDs, região, hora do deploy — infalsificável"; princípio herdado ADE-000 Inv 5 — correlationId sempre de origem de backend.)*

4. **AC-4 — Assinatura fixa: `"Squad AIOX + você"`.** Elemento textual/visual obrigatório no Diploma, exatamente como especificado no plano (não uma variação livre de copy).
   *(Fonte: plano §1 — "assinado 'Squad AIOX + você'".)*

5. **AC-5 — Elemento compartilhável (o "loop viral"):** o Diploma expõe uma ação de compartilhamento (ex.: botão "Compartilhar no LinkedIn" gerando um link/imagem pré-formatada com o resumo da telemetria do aluno) — design de `@ux-design-expert` (Uma). Não é um requisito técnico de integração de API do LinkedIn (fora de escopo inventar uma integração OAuth nova); é, no mínimo, um link de compartilhamento pré-formatado (`https://www.linkedin.com/sharing/share-offsite/?url=...`, mecanismo público e documentado do LinkedIn, sem necessidade de app registration) ou um botão de "copiar resumo" — decisão final de UX cabe a `@ux-design-expert`.
   *(Fonte: plano §1 — "→ LinkedIn/loop viral"; mecanismo de share-offsite é API pública documentada do LinkedIn, não inventada.)*

6. **AC-6 — Nenhum PII sensível exposto no Diploma além do que já é do próprio aluno:** `entra_oid` não é exibido em texto puro (mesma higiene já praticada — Story 2.3 AC-12, "oid é PII, nunca aparece em log/aplicação exposta"); o Diploma mostra o **nome do aluno** (já conhecido dele mesmo, do seu próprio login) e os correlation-IDs (identificadores técnicos, não PII de terceiros) — nenhum dado de OUTRO aluno é acessível (a query de telemetria é escopada à sessão/identidade do próprio aluno logado).
   *(Fonte: padrão herdado de higiene de PII, Story 2.3 AC-12; princípio de escopo por usuário autenticado, ADE-004/007.)*

### Bloco C — Diagrama draw.io de topologia/segurança

7. **AC-7 — Novo arquivo `docs/diagrams/final-topologia-seguranca.drawio`**, usando **ícones Azure reais** — mesmo padrão validado nas Oitavas/Quartas (reúso de pacote de ícones, não criação de iconografia nova). Documenta a malha fechada resultante das Stories 4.1/4.2: VNet, Private Endpoint (SQL, T2), Container Apps Environment VNet-integrado, Key Vault, Managed Identities (por serviço), o fluxo `X-Gateway-Key` (gateway → Functions/McpServer/backend-v1) e o fluxo `X-Entra-OID` (gateway → Functions, já existente).
   *(Fonte: EPIC-004 §Escopo Frente C — "Diagrama draw.io de topologia/segurança (ícones Azure reais)"; plano §8 — "`docs/diagrams/final-topologia-seguranca.drawio`"; padrão herdado Oitavas/Quartas.)*

8. **AC-8 — Fidelidade ao real (gate `@architect`):** o diagrama reflete **exatamente** o que as Stories 4.1/4.2 implementaram (não a aspiração T3, que é "nomeada, não construída" — ADE-009 §Priorização) — se T2 (Private Endpoint) não estiver de fato provisionado na turma corrente, o diagrama marca esse componente como "showcase/opcional", não como onipresente. Nenhum componente inventado; `@architect` confirma a fidelidade no gate desta story, espelhando o mesmo cuidado que o `story-tmpl`/Art. IV exige de qualquer AC.
   *(Fonte: ADE-009 §Priorização T1/T2/T3; Art. IV — anti-hallucination aplicado a artefatos visuais, não só código.)*

### Bloco D — Regressão e anti-hallucination

9. **AC-9 — Regressão: nenhuma rota/fluxo existente do frontend é alterado.** O Diploma é uma rota **nova** e aditiva (mesmo princípio da Story 3.1 AC-14 sobre o F6 — "não altera o fluxo existente"); `AuthProvider`/`api.ts`/`apiV2.ts` e as demais rotas (chatbot, visualizer, checkout) permanecem intocados.
   *(Fonte: EPIC-004 Compatibility Requirements — "Diploma vivo é rota/página nova... usa o stack React/Vite atual".)*

10. **AC-10 — Anti-hallucination (Art. IV):** `Azure.Monitor.Query`/`DefaultAzureCredential` (reúso confirmado do padrão FlowEvents), o mecanismo `linkedin.com/sharing/share-offsite` (API pública real do LinkedIn, se escolhida pela UX) e os ícones Azure do draw.io são reais/documentados — nenhum endpoint, claim ou ícone inventado.

---

## `[AUTO-DECISION]` — a pendência de design do épico, resolvida aqui

### Onde o Diploma vivo é servido: rota no frontend do lab (ESCOLHIDA) vs. página estática no Template Repo

**Decisão:** rota nova no frontend React **já existente** (`Lovable/World Cup Tickets Hub/`, deployado pelo aluno desde as Oitavas/Quartas), **não** uma página estática dentro do Template Repository `fifa2026-final-lab`.

**Por quê:**
- **A fonte de telemetria (App Insights/FlowEvents) exige autenticação e infraestrutura de API já existentes** no frontend atual (`AuthProvider`, `apiV2.ts`, o roteamento via gateway) — replicar isso do zero numa página estática no Template Repo seria reinventar a roda (e provavelmente inventar uma nova superfície de autenticação, contrariando Art. IV).
- **O próprio épico já aponta a branch:** a tabela de Stories lista "`phase-08-hardening` (Diploma React)" — código React implica o frontend existente, que vive nesta branch/repositório, não no Template Repo (que é infraestrutura de entrega, não código de aplicação).
- **"Surface no Template Repo" é satisfeito por link, não por duplicação:** o workflow `lab-grande-final.yml` (Story 4.5) já imprime URLs como padrão estabelecido (AC-11 da 4.5) — adicionar a URL do Diploma ao checkpoint final é suficiente para "aparecer" na experiência de entrega, sem duplicar código React num repositório de infraestrutura.

### Fonte concreta de telemetria: App Insights via FlowEvents (ESCOLHIDA) — não uma nova query SQL direta

**Decisão:** reusa `AppInsightsFlowEventRepository`/`Azure.Monitor.Query` (o mecanismo que já alimenta o F6), não uma nova consulta direta a `purchases` via SQL.

**Por quê:**
- **Reúso > invenção (Art. IV/IDS):** o F6 (EPIC-003 3.4) já resolve exatamente o problema de "telemetria real e correlacionável por aluno" — construir uma segunda fonte (SQL direto) duplicaria lógica e criaria duas versões da verdade sobre a mesma jornada de compra.
- **Consistência narrativa:** "o Diploma renderiza o estado final; o F6 já mostrou a jornada ao vivo" (EPIC-004 §Dependências — "4.6 é o último: o Diploma renderiza o estado final (telemetria do F6/EPIC-003)") — usar a MESMA fonte reforça que o Diploma é a "foto" do que o F6 já provou em movimento.
- **Alternativa registrada (não escolhida):** uma query SQL direta em `purchases` seria mais simples de implementar, mas duplicaria a fonte de verdade e não reusaria a MI/padrão já validado pela Story 4.1/F6 — rejeitada por essa razão.

---

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** Frontend (nova rota/página React)
**Secondary Type(s):** Integration (consulta a App Insights/telemetria real), Architecture (draw.io — fidelidade documental)
**Complexity:** MEDIUM (a rota em si é padrão React; a parte não-trivial é a consulta agregada de telemetria e a garantia de infalsificabilidade — dados vindos do backend, não fabricados no cliente)

### Specialized Agent Assignment

**Primary Agents:**
- @dev (Dex) — rota/página do Diploma + consulta de telemetria
- @ux-design-expert (Uma) — design "viral" (compartilhamento)
- @analyst (Atlas) — draw.io

**Supporting Agents:**
- @pm (Morgan) — gate de experiência
- @architect (Aria) — gate de fidelidade do diagrama

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): `coderabbit --prompt-only -t uncommitted` — foco em nenhum dado de telemetria fabricado no frontend (deve vir do backend)
- [ ] Pre-PR (@devops): `coderabbit --prompt-only --base main` — regressão de rotas existentes
- [ ] Pre-Deployment: N/A — story de frontend/documentação, sem infraestrutura nova

### Self-Healing Configuration

**Expected Self-Healing:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL

**Predicted Behavior:**
- CRITICAL issues: auto_fix
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus:**
- Dados do Diploma (correlation-IDs, região, timestamp) vêm de uma resposta de API/build metadata — nunca `Math.random()`/hardcoded/gerado no cliente
- Nenhum PII de outro aluno acessível (query escopada ao usuário autenticado)

**Secondary Focus:**
- Acessibilidade da página (WCAG 2.1 AA — herdado do padrão de frontend do projeto)
- `entra_oid` não exposto em texto puro na UI

---

## Tasks / Subtasks

### Task 1 — Branch e localização (AC: 1)

- [x] 1.1 Confirmar continuidade em `phase-08-hardening` para o código React do Diploma. *(código React vive no frontend existente `Lovable/World Cup Tickets Hub/`; a worktree do @dev é `lab-a-final`, mesma linha de trabalho da Grande Final — o merge/branch fica com o coordenador.)*
- [x] 1.2 Criar pasta `docs/diagrams/` neste repositório (se ainda não existir). *(já existe — o @analyst criou `docs/diagrams/final-topologia-seguranca.drawio` em paralelo; @dev não tocou a pasta.)*

### Task 2 — Diploma vivo: telemetria (AC: 2, 3, 6)

- [x] 2.1 Implementar/estender uma consulta agregada reusando `AppInsightsFlowEventRepository` (ou endpoint equivalente exposto via gateway) para retornar correlation-IDs recentes da sessão do aluno. *(endpoint aditivo `GET /api/flow/diploma-summary` + `GetPurchasesByUserAsync` — mesma fonte App Insights do F6, só escopada.)*
- [x] 2.2 Resolver região/timestamp de deploy a partir de metadados de build/ambiente (não fabricado no cliente). *(região = env do FlowEvents `DeployRegion`/`REGION_NAME`; deployTime = `VITE_BUILD_TIME` baked no bundle pelo CI — verificado no chunk `Diploma-*.js`.)*
- [x] 2.3 Confirmar escopo por usuário autenticado (nenhum dado de outro aluno vaza). *(filtro Kusto `customDimensions.UserId == userId`; endpoint só devolve region + correlation-IDs + count.)*
- [x] 2.4 Confirmar `entra_oid` não exposto em texto puro na UI. *(o endpoint nunca retorna `oid`; a UI nunca o busca/renderiza/copia/OG.)*

### Task 3 — Diploma vivo: página e assinatura (AC: 1, 4, 5)

- [x] 3.1 Criar a rota/página nova (`/diploma` ou equivalente) no frontend React existente. *(`src/pages/Diploma.tsx` + rota lazy em `App.tsx` + link no `Navbar`.)*
- [x] 3.2 Adicionar a assinatura fixa `"Squad AIOX + você"`. *(constante `SIGNATURE`, string literal exata.)*
- [x] 3.3 (@ux-design-expert) Desenhar o elemento de compartilhamento (LinkedIn share-offsite ou equivalente). *(spec da Uma implementada 1:1: LinkedIn `share-offsite` + "Copiar resumo" com o texto pt-BR exato.)*

### Task 4 — Draw.io (AC: 7, 8)

- [x] 4.1 (@analyst) Criar `docs/diagrams/final-topologia-seguranca.drawio` com ícones Azure reais, documentando VNet/PE/CAE-VNet-integrado/KV/MI/`X-Gateway-Key`/`X-Entra-OID`. *(arquivo criado pelo @analyst em `docs/diagrams/final-topologia-seguranca.drawio`; conteúdo/fidelidade não é escopo do @dev.)*
- [x] 4.2 (@architect) Validar fidelidade ao estado real (T1 vs T2 — marcar T2 como showcase/opcional se não estiver sempre provisionado). *(gate @architect — **PASS**, 2026-07-02, Aria. Fidelidade confirmada componente-a-componente vs ADE-009/010 + Stories 4.1/4.2; T2 marcado showcase/opcional, T3 só nomeado. 2 refinamentos de honestidade opcionais/não-bloqueantes para o @analyst — ver §QA Results.)*

### Task 5 — Handoff à Story 4.5 (surface no Template Repo)

- [x] 5.1 Confirmar com `@devops` (Story 4.5) a inclusão da URL do Diploma no checkpoint final de `lab-grande-final.yml`. *(`[AUTO-DECISION]` — implementado direto: linha `Diploma: .../diploma` adicionada ao job summary do bloco `frontend`. Re-sync do repo real `TFTEC/fifa2026-final-lab` fica com @devops — ver Dev Agent Record.)*

### Task 6 — Regressão e anti-hallucination (AC: 9, 10)

- [x] 6.1 Confirmar `AuthProvider`/`api.ts`/`apiV2.ts`/demais rotas intocados. *(nenhum desses arquivos tocado; rota `/diploma` é 100% aditiva; build verde.)*
- [x] 6.2 Revisão anti-hallucination (fonte de telemetria, mecanismo de share, ícones do draw.io). *(Azure SDK reusado; `linkedin.com/sharing/share-offsite` é API pública real; ícones do draw.io = @analyst.)*

---

## Dev Notes

### Arquitetura técnica delta

```
[Lovable/World Cup Tickets Hub/ — frontend React existente]
  + rota nova /diploma (ou equivalente)
  + consulta a telemetria real (reusa AppInsightsFlowEventRepository / Azure.Monitor.Query — mesmo padrão MI da 4.1)
  + assinatura "Squad AIOX + você" + elemento de compartilhamento

[docs/diagrams/ — pasta nova, este repositório]
  + final-topologia-seguranca.drawio (ícones Azure reais; VNet/PE/MI/KV/X-Gateway-Key)

[.github/workflows/lab-grande-final.yml — Story 4.5]
  checkpoint final passa a incluir a URL do Diploma (handoff, Task 5)
```

### Arquivos tocados (File List esperada)

| Arquivo | Ação |
|---|---|
| `Lovable/World Cup Tickets Hub/src/pages/Diploma.tsx` (nome sugerido) | **Novo** |
| `Lovable/World Cup Tickets Hub/src/App.tsx` (ou equivalente de rotas) | Modificado (+ rota) |
| Endpoint de telemetria agregada (backend, se necessário — a decidir por `@dev`) | **Novo** ou reúso direto |
| `docs/diagrams/final-topologia-seguranca.drawio` | **Novo** |

**NÃO tocam:** rotas/fluxos existentes do frontend (chatbot, visualizer, checkout); `AppInsightsFlowEventRepository.cs` em si (é consumido, não reescrito, a menos que precise de um método novo de agregação — nesse caso, aditivo).

### Verificação na fonte da verdade (Art. IV)

| Afirmação | Confirmado em |
|---|---|
| `AppInsightsFlowEventRepository` usa `Azure.Monitor.Query` + `DefaultAzureCredential` | `Fifa2026.V2.FlowEvents.csproj` (dependências) |
| Padrão de higiene de PII para `entra_oid` (nunca exposto/logado) | Story 2.3 AC-12 (herdado, citado em múltiplas stories) |
| Plano cita "Diploma do Sistema... correlation-IDs, região, hora do deploy — infalsificável, assinado 'Squad AIOX + você'" | `giggly-mapping-hearth.md` §1 |

### Testing

**Test file locations:** testes de frontend seguem o débito já sinalizado no épico ("Frontend com ZERO testes automatizados... candidato a AC mínima em 4.6 OU story transversal futura — decisão de @sm/@po"). **Decisão desta story:** não introduzir um framework de teste de frontend do zero apenas para o Diploma (escopo desproporcional a uma rota nova) — o débito de frontend-zero-testes **permanece sinalizado**, não resolvido aqui (registrado no Change Log e no DoD do épico como item explicitamente diferido).

**Test standards:** validação manual/observada (dry-run do Diploma com uma sessão real de compra) é o critério de aceite desta story.

---

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-07-03 | 1.0 | **Emenda MEDIUM-4 FECHADA — gates PASS + review adversarial APROVADO (3 rodadas).** Rodada 3 (2 ajustes baratos do review delta): **UX-1** — a CTA de login do estado-gate do `/diploma` sumia no mobile (o `LoginV2Button` da navbar é `hidden lg:flex`); trocada por um botão próprio do gate que reusa `msalInstance.loginPopup(loginRequest)`, **visível em todos os breakpoints** (navbar intocada). **N-1** — adicionado o teste-âncora `NoToken_OnDiplomaSummary_Returns401` (Gateway) que **falharia** se a rota virasse anônima (fecha a nota LOW N-1 do gate @qa). Gates finais: **@architect PASS 6/6**, **@qa PASS 7/7**, review adversarial **APROVADO**. **Testes: Gateway 42/42 (+1), FlowEvents 26/26, front tsc exit 0 / eslint 0/0 / build verde.** Story segue **Ready** (Done na validação ao vivo). Re-sync `TFTEC/fifa2026-final-lab` = @devops. | @dev (Dex) |
| 2026-07-03 | 0.9 | **Gate `@qa` (Quinn) da Emenda MEDIUM-4 = ✅ PASS.** Ângulo QA (testes provam o que dizem · ACs cobertos · regressão · OWASP básica), complementar ao gate `@architect`. 7 checks PASS: (1) **resistência a mutação** — os 3 testes novos falhariam se a proteção fosse inerte (401 sem/errada key, injeção+anti-spoof do gateway, forjado descartado); **26/26 + 41/41 reproduzidos nesta máquina** (`dotnet test`); (2) todos os `AC-M4-1..7` com evidência (único gap de teste = AC-M4-1 front, débito frontend-0-testes sancionado); (3) retro-compat — bypass legado coberto pelos 2 testes de segredo-vazio, `FlowEvents_Cluster_Does_NOT_Receive_GatewayKey` verde, F6/`/recent`/timeline/SignalR intocados; (4) OWASP — `FixedTimeEquals` correto, segredo **não** vaza em log/resposta/bundle (o `LogWarning` do MEDIUM-2 não imprime o valor), fail-closed OK, anti-spoof global+route-scoped; (5) gate CIAM cobre nato-CIAM/só-v1/sem-config sem loading infinito; (6) perf N/A (1 `meV2()` extra, cacheado); (7) guia + `final-security-debt.md` coerentes com o código. 4 notas LOW/INFO não-bloqueantes (N-1 teste de 401-sem-Bearer opcional; N-2 front sem teste; N-3 produto só-v1→@pm; N-4 timing de length-mismatch aceito). Parecer completo em §QA Results. **Status inalterado (Ready).** | @qa (Quinn) |
| 2026-07-03 | 0.8 | **Gate `@architect` (Aria) da Emenda MEDIUM-4 = ✅ PASS.** Validada a fidelidade da implementação do @dev ao design Opção F e à ADE-009 v1.1 (emenda route-scoped à Inv 1). 6 checks PASS: (1) rota `flow-events-diploma` + transform route-scoped por `RouteId` (molde `me-get`, header distinto `X-Diploma-Key` = `adminSharedSecret`), herda o blanket `RequireAuthorization` (`Program.cs:684-686`); (2) FlowEvents valida timing-safe (`FixedTimeEquals`) SÓ no `diploma-summary`, vazio=bypass, 4 endpoints intactos; (3) **retro-compat provada por teste EXECUTADO** (dotnet 10.0.301 — FlowEvents 26/26, Gateway 41/41; `FlowEvents_Cluster_Does_NOT_Receive_GatewayKey` + `DiplomaRoute_Injects_XDiplomaKey_ButNever_XGatewayKey` verdes; `gatewayKeyClusters` intocado; ingress `external`); (4) draw.io edge `eGwFlowDiploma` + nota, XML well-formed (`[xml]` .NET); (5) correções da rodada 2 (gate CIAM via `meV2()`, LogWarning fail-loud operator-facing, strip global) arquiteturalmente sãs; (6) nota de produto (`/diploma` exige CIAM) = **ACEITA** arquiteturalmente (coerente com identidade base v1↔CIAM da Final), com a decisão de produto (caminho só-v1?) encaminhada ao @pm como não-bloqueante. Parecer completo em §QA Results. **Status inalterado (Ready)** — pendem code review adversarial (M4-5.1), gate @qa (M4-5.2), re-sync @devops e validação ao vivo. | @architect (Aria) |
| 2026-07-03 | 0.7 | **Correções do review adversarial da Opção F (APROVADO C/ RESSALVAS → fechado).** **MEDIUM-1** (split de identidade v1↔CIAM — bug funcional que a rodada 1 introduziu): `Diploma.tsx` passou a gatear na **sessão CIAM** (`useIsAuthenticated`) em vez do auth v1, e a resolver o `userId` via **`meV2()`** (mesma resolução do Checkout/Story 3.5 → casa com `customDimensions.UserId` da telemetria e cobre o nato-CIAM). Conserta o aluno só-v1 caindo em 401+popup e o mismatch userId↔telemetria. **Consequência de produto reportada** (não decidida): a Opção F faz o `/diploma` exigir Login v2/CIAM — se o owner quiser Diploma p/ só-v1, conflita com a Opção F. **MEDIUM-2** (segredo esquecível): `FlowEvents/Program.cs` emite WARNING de startup se `DiplomaSharedSecret` vazio em produção (fail-loud, não no /health público); verificação no guia Fase 7.4. **LOW**: `X-Diploma-Key` no strip anti-spoof GLOBAL do gateway. **Testes: FlowEvents 26/26, Gateway 41/41, front tsc/eslint/build verdes.** Story segue **Ready**. | @dev (Dex) |
| 2026-07-03 | 0.6 | **Emenda MEDIUM-4 IMPLEMENTADA (Opção F — defesa em profundidade).** `@dev` executou `AC-M4-1..7` / tasks `M4-1..M4-4`. **Gateway:** rota `flow-events-diploma` (antes do catch-all) + transform route-scoped injetando `X-Diploma-Key` (reuso do `adminSharedSecret` sob header distinto, molde `me-get`); `gatewayKeyClusters` intocado. **FlowEvents:** `FlowEndpoints.cs` valida `X-Diploma-Key` timing-safe (`FixedTimeEquals`) SÓ no `diploma-summary`, fail-closed com segredo armado / bypass legado com segredo vazio. **Frontend:** `fetchDiplomaSummary` passa a ir pelo `VITE_GATEWAY_V2_URL` com `Authorization: Bearer` (conserta de quebra o H-2). **Docs:** guia Fase 7.4 (flip)/9 (amarra)/Apêndice D; draw.io edge `X-Diploma-Key`; `final-security-debt.md` MEDIUM-4 → **FECHADO**; ADE-009 já emendada (v1.1). **Retro-compat:** ingress `external` do `ca-flow` intocado, `/recent`/timeline/SignalR intocados, `FlowEvents_Cluster_Does_NOT_Receive_GatewayKey` verde. **Testes: FlowEvents 26/26 (+2), Gateway 41/41 (+1), front build verde + eslint 0/0.** 1 desvio da emenda: a chave-comentário `"// …"` sob `Routes` foi removida (YARP a lê como rota → startup falha) — doc migrada ao `Program.cs`. **Status inalterado (Ready)** — Done só na validação ao vivo; gates `@architect`/`@qa` + code review adversarial pendentes (Task M4-5). Re-sync `TFTEC/fifa2026-final-lab` = @devops. | @dev (Dex) |
| 2026-07-03 | 0.5 | **Emenda MEDIUM-4 (aprovada pelo owner 2026-07-03) — anexada.** O owner **rejeitou aceitar o débito** (Change Log 0.3 / `final-security-debt.md` MEDIUM-4) e pediu proteção real. `@architect` (Aria) desenhou e o owner aprovou a **Opção F (defesa em profundidade)**: o cliente `/diploma` passa a mandar **Bearer** (via gateway); o gateway injeta `X-Diploma-Key` **route-scoped** (reúso do segredo da Fase 9, header distinto); o FlowEvents valida a key timing-safe **só** no `diploma-summary`; `/recent`/SignalR e o **ingress `external`** do `ca-flow` **intocados**. Emenda técnica completa (contexto + design + `AC-M4-1..7` + tasks com caminhos-âncora + impacto no guia/draw.io + residual) adicionada em §"Emenda MEDIUM-4" ao fim da story, pronta para `@dev`. ADE-009 emendada em paralelo (v1.1 — exceção route-scoped à Inv 1). **Escopo:** só design/doc — nenhum código tocado nesta rodada. Story permanece **Ready** (a emenda é uma nova frente de trabalho para o @dev; o corpo original da 4.6 segue como estava). | @architect (Aria) |
| 2026-07-01 | 0.1 | Story drafted — Capstone-experiência (Frente C do EPIC-004, última story do épico). 10 ACs derivados do plano §1 + EPIC-004 §Escopo Frente C. Resolvidas as 2 pendências de design explicitamente deixadas pelo épico (`[AUTO-DECISION]`): (a) Diploma = rota no frontend React existente, não página estática no Template Repo — reusa infraestrutura de auth/API já existente, "surface" via link no checkpoint da Story 4.5; (b) fonte de telemetria = reúso do `AppInsightsFlowEventRepository`/F6 (EPIC-003 3.4), não uma nova query SQL direta — evita duas fontes de verdade sobre a mesma jornada. Débito de frontend-zero-testes explicitamente **não resolvido** nesta story (sinalizado, decisão consciente de escopo). | @sm (River) |
| 2026-07-02 | 0.4 | **Bookkeeping final + gates PASS.** Gate @architect (Aria) = **PASS** (fidelidade do draw.io à ADE-009/010 + sanidade do delta FlowEvents + retro-compat; checkbox 4.2 marcada; 2 nits R-1/R-2 opcionais). Gate @pm (Morgan) = **PASS** (experiência/loop viral; referenda deferimentos html2canvas/OG + débito MEDIUM-4). Ambos os pareceres colados em §QA Results. Ajustes desta rodada (só doc/label/summary — zero mudança funcional): LOW-1 `/og-image.png` confirmado existente (mantido; URL absoluta segue como limitação L-1); LOW-3 linha do Diploma no job summary tornada celebratória (🏆); R-1/R-2 aplicados no `final-topologia-seguranca.drawio` (rótulos parentetizados "não versionado em IaC" — XML revalidado well-formed); INFO registrada no Dev Agent Record (divergência "você"/"eu" é intencional). YAML e XML revalidados. **Status inalterado (Ready)** — decisão Done fica com o coordenador/validação ao vivo. | @dev (Dex) |
| 2026-07-02 | 0.3 | **Correções pós code-review adversarial (doc/comentário apenas).** H-1: reescrito o parágrafo de risco residual (a claim "gateway autentica → só logados" era **factualmente falsa**; superfície real = anônima no ingress externo do `ca-flow`, `userId` enumerável, cliente sem Bearer; delta real = vínculo userId→GUIDs opacos já expostos pelo `/flow/recent` desde a 2.6; zero PII) + comentários espelhados corrigidos em `Diploma.tsx`/`FlowEndpoints.cs` + débito formal **MEDIUM-4** em `final-security-debt.md`. H-2: registrado o gap gateway-401-vs-FQDN-direto + item E2E na lista de validação ao vivo da Story 3.3 (Architect Gate, item 5). L-1: limitação das OG tags (Helmet/SPA sem SSR + og:image relativa) registrada. L-3: checkboxes 1.2/4.1 marcadas (draw.io existe), 4.2 pendente (gate @architect). | @dev (Dex) |
| 2026-07-02 | 0.2 | **Implementado (@dev / Dex) — Tasks 1/2/3/5/6** (Task 4/draw.io = @analyst, paralelo). Diploma vivo = rota React nova `/diploma` (spec da Uma 1:1: 5 estados, selos de autenticidade, LinkedIn share-offsite + Copiar resumo, a11y, PII). Escopo por aluno (AC-6) via `customDimensions.UserId` **já presente** na telemetria do Entry → endpoint aditivo `GET /api/flow/diploma-summary` (FlowEvents, mesma fonte App Insights do F6 — AC-2). `region` = env do backend; `deployTime` = `VITE_BUILD_TIME` baked no bundle pelo CI (infalsificável — AC-3, verificado no chunk). Assinatura literal `Squad AIOX + você` (AC-4). Aditivo/retro-compat (AC-9): `AuthProvider`/`api.ts`/`apiV2.ts`/rotas intocados. FlowEvents 24/24 testes; frontend build verde; eslint 0/0; YAML válido. Débito frontend-0-testes diferido (sancionado). Risco residual do escopo por `userId` client-side documentado no Dev Agent Record. Status inalterado (Ready) — aguardando gates @pm (experiência) + @architect (draw.io). | @dev (Dex) |
| 2026-07-01 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready. Última story do épico; 2 `[AUTO-DECISION]`s resolvem pendências deixadas pelo épico (Diploma = rota no frontend existente; telemetria = reúso do `AppInsightsFlowEventRepository`/F6). Capstone in-épico confirmado (ver 4.5). Leve dedução: UI nova (Diploma) entra sem testes automatizados — débito frontend-0-testes conscientemente diferido e sinalizado (sancionado pelo épico), não gap silencioso. AC-3 infalsificabilidade (dado do backend/build, não do cliente) bem especificada. | @po (Pax) |

---

## Dev Agent Record

### Agent Model Used

Dex (@dev) — Opus 4.8 (1M). Implementação em 2026-07-02 na worktree `lab-a-final`. Tasks 1/2/3/5/6 (Task 4/draw.io = @analyst, em paralelo — não tocada).

**Emenda MEDIUM-4 (2026-07-03):** Dex (@dev) — `claude-fable-5`. Implementação na worktree `story-3.2-n8n-decommission` (HEAD `63cebbd`). Tasks `M4-1..M4-4` (gates `M4-5` pendentes).

### Completion Notes — Emenda MEDIUM-4 (Opção F)

**Design implementado 1:1 com a emenda (ADE-009 v1.1):** duas provas independentes que o anônimo da internet não satisfaz — **identidade** (Bearer via gateway → blanket `RequireAuthorization` barra o anônimo) + **proveniência** (`X-Diploma-Key` route-scoped que o FlowEvents valida timing-safe).

1. **Gateway — rota + transform route-scoped.** `appsettings.json`: rota `flow-events-diploma` (`/flow-events/api/flow/diploma-summary`, GET, `PathSet` → `/api/flow/diploma-summary`) **antes** do catch-all `flow-events-api`; sem `AuthorizationPolicy` explícito → herda o blanket. `Program.cs`: consts `DiplomaRouteId`/`DiplomaKeyHeader` + bloco `if (Route?.RouteId == "flow-events-diploma")` (molde exato do `me-get`) com anti-spoof (`Remove`) + injeção condicional do `adminSharedSecret` (já carregado na l.88). **`gatewayKeyClusters` NÃO tocado** — o cluster `flow-events` segue fora do `X-Gateway-Key`.
2. **FlowEvents — validação route-scoped.** `FlowEndpoints.cs`: o handler `diploma-summary` recebe `HttpRequest`, lê `DiplomaSharedSecret`; setado → exige `X-Diploma-Key` em `CryptographicOperations.FixedTimeEquals` → 401 se ausente/divergente; vazio → **bypass legado** (mesma filosofia do `GatewayKeyValidator.cs:51-54`). Helper `IsTrustedDiplomaKey` local (o FlowEvents não referencia o projeto Functions; a lógica timing-safe é 4 linhas, reusar exigiria acoplamento novo — decisão de reúso-vs-acoplamento). Os outros 4 endpoints intocados.
3. **Frontend.** `flowApi.ts`: `fetchDiplomaSummary` migrou para `VITE_GATEWAY_V2_URL` + `Authorization: Bearer` (`getV2AccessToken`, o mesmo accessor do `apiV2.ts`). `/recent`/timeline/replay/hub seguem na `FLOW_BASE` (anônimos, MEDIUM-1). `Diploma.tsx`: só comentários (POSTURA REAL + `enabled`); contrato inalterado.
4. **Bônus (H-2 consertado):** antes o `fetchDiplomaSummary` **não** mandava Bearer → via gateway o `/diploma` daria 401 (item 5 da validação ao vivo da 3.3). Agora manda → funciona via gateway como deveria.

**Correção da postura anterior (o parágrafo "Risco residual" da 4.6 v0.2/0.3 está SUPERSEDED):** aquele texto descrevia o endpoint como **anônimo/débito aceito**. Com a Opção F isso deixou de ser verdade: o `/diploma-summary` agora exige Bearer (via gateway) + `X-Diploma-Key` (proveniência). O **residual** encolheu para *enumeração autenticada intra-lab* (aluno logado passando `?userId=<outro>` → GUIDs opacos + count, **zero PII**, igual/menor que o `/recent`). O escopo infalsificável-por-identidade = **Opção D** (gateway injeta `X-Entra-OID`; FlowEvents resolve `oid→userId`), **condicionada a verificar** se a telemetria carrega o `oid` (hoje `customDimensions.UserId` é o int v1 — Art. IV, não assumir) → **fast-follow**.

**Desvio da emenda (1, justificado):** a emenda (M4-1.1) sugeria anotar a rota com uma chave `"// …"` dentro de `ReverseProxy:Routes`. **Não é viável:** o YARP interpreta **toda** chave sob `Routes` como uma rota e falha no startup (`Route '// …' requires Hosts or Path specified` — reproduzido: 34/40 testes do Gateway quebraram até eu remover a chave). Diferente do topo do `appsettings.json`, onde chaves `"// …"` sob `Gateway`/raiz são inertes. A documentação foi para o `Program.cs` (consts `DiplomaRouteId`/`DiplomaKeyHeader` comentadas). Zero impacto funcional.

**Verificação:** FlowEvents `dotnet test` = **26/26** (+2: `DiplomaSummary_with_correct_key_returns_200_when_secret_set`, `DiplomaSummary_rejects_missing_or_wrong_key_when_secret_set`). Gateway `dotnet test` = **41/41** (+1: `DiplomaRoute_Injects_XDiplomaKey_ButNever_XGatewayKey`; `FlowEvents_Cluster_Does_NOT_Receive_GatewayKey` segue verde). Frontend `npm run build` = verde; `eslint src/lib/flowApi.ts src/pages/Diploma.tsx` = **0/0**. Draw.io revalidado well-formed (`[xml]` PowerShell).

**Pontos de atenção p/ o code review adversarial:** (a) o segredo `X-Diploma-Key` **nunca** é logado (nenhum `logger` no caminho) nem vai ao bundle (é injetado server-side pelo gateway; o front só manda o Bearer); (b) semântica fail-closed correta — segredo vazio = bypass **intencional** (dev local/pré-provisionamento), não um buraco; (c) o header é **distinto** do `X-Gateway-Key` e a injeção é **por rota**, então o cluster `flow-events` segue fora do escopo cluster-scoped (retro-compat F6); (d) `IsTrustedDiplomaKey` retorna `false` para header vazio/nulo (fail-closed quando o segredo está armado); (e) validação de `userId` (`IsValidUserId`, dígitos ≤12) + Kusto parametrizado permanecem — o key gate é camada adicional, não substitui os guards existentes.

### Completion Notes — Rodada 2 (correções do review adversarial da Opção F, 2026-07-03)

O review voltou **APROVADO C/ RESSALVAS**: núcleo de segurança fechado, mas 2 MEDIUMs + 1 LOW a fechar antes dos gates. Todos fechados nesta rodada.

**MEDIUM-1 (split de identidade v1↔CIAM — RESOLVIDO no padrão v2 existente, NÃO virou decisão de produto nova).** *Investigação:* o hub tem **dois mundos de auth paralelos** — v1 (`useAuth`/AuthProvider, email+senha, localStorage) e CIAM (`@azure/msal-react`/`MsalProvider`). O **Checkout** (a compra que gera a telemetria) resolve o `userId` assim: `let userId = Number(user?.id); const me = await meV2(); if (me.userId) userId = me.userId;` — ou seja, **a telemetria (`customDimensions.UserId`) é o int do `/api/v2/me` (CIAM), não o id v1**. Minha rodada 1 tinha 2 bugs herdados/introduzidos: (i) passava o **id v1** (`user.id`) ao endpoint — **mismatch** com a telemetria (nato-CIAM não tem id v1; vinculado tem, mas por coincidência de o `/me` devolver o mesmo id); (ii) mandava Bearer via `getV2AccessToken()` mas **gateava por auth v1** → aluno só-v1 disparava a query sem token → 401 + risco de `acquireTokenPopup` surpresa. **Correção (reúso puro do padrão Story 3.5/Checkout):** o gate `enabled` passou a exigir a **sessão CIAM** (`useIsAuthenticated()` + `isEntraConfigured()`), e o `userId` é **resolvido via `meV2()`** dentro da `queryFn` — o MESMO resolver que a compra usa, então o escopo **casa com a telemetria** e cobre o **nato-CIAM**. Sem sessão CIAM → estado-gate "Login v2 (CIAM)" com `LoginV2Button` (popup **iniciado pelo aluno**, nunca no load). **Não** toquei `AuthProvider`/`apiV2.ts`/`authV2.ts` — só `Diploma.tsx`. **⚠️ NOTA DE PRODUTO (para o coordenador levar ao owner se quiser):** como o endpoint agora exige Bearer-de-cliente (= token CIAM, consequência DIRETA da Opção F já aprovada), o `/diploma` **passa a exigir Login v2/CIAM** — um aluno que comprou **só via v1** (sem nunca fazer Login v2) **não vê o Diploma** (vê o gate pedindo CIAM). Isso **não é invenção minha**: é o corolário da Opção F (Bearer via gateway). Se o owner **quiser** que o só-v1 também receba Diploma, aí sim vira decisão de produto que **conflita** com a Opção F (exigiria um caminho não-autenticado) — **reportado, não decidido por mim**. No fluxo real da Grande Final o aluno faz o Login v2/CIAM (é o tema "identidade base v1↔CIAM"), então o caminho feliz é o CIAM.

**MEDIUM-2 (eficácia contingente a segredo esquecível — RESOLVIDO com sinal).** Se o operador armar o gateway (Fase 9) mas **esquecer** o `DiplomaSharedSecret` no `ca-flow` (Fase 7.4), o FlowEvents cai no bypass legado → MEDIUM-4 reabre **anônimo sem sinal** (via gateway tudo aparenta ok). **Correção:** `FlowEvents/Program.cs` emite um `LogWarning` alto no **startup** quando `DiplomaSharedSecret` está vazio em ambiente **não-Development** (dev local vazio = silencioso, intencional). **Fail-LOUD, não fail-closed** — não derrubo o serviço (recent/timeline/SignalR seguem; o pré-provisionamento pode ainda não ter armado). **Decisão:** NÃO expus isso no `/health` público (o `ca-flow` tem ingress externo → sinalizaria a um atacante que o endpoint está aberto); o sinal é **operator-facing** (logs do Container App). Verificação registrada no guia Fase 7.4 ("ausência do warning = trava armada").

**LOW (anti-spoof global — RESOLVIDO).** O `Program.cs` removia `X-Diploma-Key` só na rota diploma; adicionei ao **strip anti-spoof GLOBAL** (junto de X-Entra-OID/Email/Name). O cliente não forja o header em nenhuma rota; a rota diploma reinjeta o valor real depois (ordem de registro). Não explorável hoje (só a rota diploma valida), mas consistente com os headers de identidade.

**Verificação rodada 2:** FlowEvents **26/26**, Gateway **41/41** (inclui `FlowEvents_Cluster_Does_NOT_Receive_GatewayKey` verde), frontend `tsc --noEmit` **exit 0** + `eslint Diploma.tsx/flowApi.ts` **0/0** + `npm run build` verde.

### Completion Notes — Rodada 3 (últimos 2 ajustes do review; gates já PASS)

Review delta **APROVADO C/ RESSALVAS** com só 2 itens baratos; fechados.

**UX-1 (quebra em mobile — RESOLVIDO).** No estado-gate do `/diploma` eu usava `<LoginV2Button />` (o botão da navbar), que tem `className="hidden lg:flex"` → **sumia abaixo do breakpoint `lg`**: no celular o aluno via a mensagem do gate mas **nenhuma CTA de login**. **Correção (opção b do review):** troquei por um botão **próprio do gate** que reusa o MESMO fluxo (`msalInstance.loginPopup(loginRequest)` via `useMsal()`) — sem `hidden lg:flex`, então **visível em mobile E desktop**. A navbar **não foi tocada** (lá o `hidden lg:flex` é intencional). Confirmação lógica: o `GateCard` renderiza `action` incondicionalmente e o botão é um `<Button className="gap-2">` sem classe de ocultação responsiva → clicável em todos os breakpoints. (Removi o import de `LoginV2Button`; adicionei `useMsal` + `loginRequest`.)

**N-1 (teste-âncora de 401 sem Bearer — ADICIONADO).** Era viável. Adicionei `NoToken_OnDiplomaSummary_Returns401` em `GatewayKeyInjectionTests.cs` (espelha o `CiamOnlyFenceTests.NoToken_OnMe_Returns401`): `_fixture.CreateClient()` sem header de auth → `GET /flow-events/api/flow/diploma-summary?userId=1` → **401**. O backend nunca é chamado (o blanket `RequireAuthorization` barra antes do proxy), então não precisa de stub. **Falharia** se alguém tornasse a rota anônima (AllowAnonymous / remoção do RequireAuthorization) — é a âncora que o @qa pediu para a prova de identidade da Opção F.

**Verificação rodada 3:** Gateway **42/42** (+1 N-1), FlowEvents **26/26**, frontend `tsc --noEmit` **exit 0** + `eslint Diploma.tsx/flowApi.ts` **0/0** + `npm run build` verde. **Emenda MEDIUM-4 fechada** (gates @architect PASS 6/6 + @qa PASS 7/7 + review adversarial APROVADO após 3 rodadas).

### Completion Notes

**Decisões-chave (Art. IV — cada fonte de dado rastreada):**

1. **Escopo por usuário (AC-6) — a fonte já existia.** O `PurchaseEntryFunction` já loga `userId={UserId}` como **parâmetro estruturado** em cada trace de entrada → o App Insights persiste `customDimensions.UserId`. Logo, o escopo do Diploma **não inventa nada**: adicionei a query Kusto `ByUserQuery` (filtra `tostring(customDimensions.UserId) == userId`) — a MESMA fonte/telemetria do F6 (AC-2), só filtrada. **Não** houve nova query SQL nem nova superfície de auth.
2. **Endpoint aditivo `GET /api/flow/diploma-summary?userId={id}`** (FlowEvents) — sancionado pela tarefa ("um endpoint aditivo mínimo… é aceitável"). Retorna `{ region, correlationIds, count }`. Aditivo: os 4 endpoints existentes (`/recent`, `/{id}`, `/{id}/replay`, `/health`) e o contrato de `IFlowEventRepository` foram **estendidos, nunca alterados** (novo método `GetPurchasesByUserAsync`; os 2 implementadores — real + fake de teste — atualizados).
3. **Fonte de `region` (AC-3, infalsificável):** resolvida **no backend** a partir do env do FlowEvents (`configuration["DeployRegion"] ?? configuration["REGION_NAME"]`). Ausente → `null` e a UI degrada graciosamente ("—"); **nunca fabricada no cliente**. `DeployRegion` é um App Setting opcional que o pré-provisionamento pode semear (mesmo padrão dos demais App Settings do FlowEvents); sem ele o Diploma continua honesto.
4. **Fonte de `deployTime` (AC-3, infalsificável):** `VITE_BUILD_TIME` **injetada no build** (`export VITE_BUILD_TIME="$(date -u …)"` no step de build dos DOIS workflows de lab). Nasce no runner do CI, é **baked no bundle** (verificado: a string do timestamp aparece no chunk `dist/assets/Diploma-*.js`), então o navegador do aluno **não consegue fabricá-la**. Ausente (build local) → UI mostra "—" (nunca um `Date.now()` client-side).
5. **`html2canvas` ("Baixar imagem") — DEFERIDO.** A dep existe no `package.json`, mas capturar o cartão dark com `gold-text`/gradientes CSS-var via html2canvas é frágil (a spec da Uma marca como *stretch, fora do caminho crítico*). Cortado para manter a mudança enxuta e sem captura flaky — decisão consciente, não gap.

**Risco residual** *(⚠️ SUPERSEDED pela Emenda MEDIUM-4 / v0.6 — o endpoint deixou de ser anônimo: agora exige Bearer via gateway + `X-Diploma-Key`. Ver "Completion Notes — Emenda MEDIUM-4" acima. Parágrafo mantido por rastreabilidade histórica.)* **(CORRIGIDO após code review adversarial — a claim anterior era factualmente falsa):** a superfície REAL do `/api/flow/diploma-summary` é **anônima**, não autenticada. Prova no código: (a) o `ca-flow`/FlowEvents tem ingress **externo**, **não** revalida JWT e está **fora** do escopo do `X-Gateway-Key` (ADE-009 Inv 1 — teste `FlowEvents_Cluster_Does_NOT_Receive_GatewayKey`), então no FQDN direto `/api/flow/diploma-summary?userId=N` é **anônimo na internet, com `userId` sequencial enumerável**; (b) mesmo via gateway, o cliente (`fetchDiplomaSummary`, como `fetchRecentPurchases`) **não envia Bearer**. O gate `enabled: isAuthenticated` do React é só de UX (client-side) e **não** protege o endpoint. **Minha afirmação original** ("o gateway autentica o Bearer → só usuários logados alcançam o endpoint") estava **errada** e foi corrigida aqui e nos comentários do código. **Delta de risco real desta story:** apenas o *vínculo* `userId`→GUIDs opacos — e o `/api/flow/recent` (F6, Story 2.6 Done) já expõe os MESMOS GUIDs **sem escopo** a qualquer chamador desde 2026. A resposta tem **zero PII** (só region + count + GUIDs opacos — sem nome/e-mail/`oid`/valor de compra). Registrado formalmente como **MEDIUM-4** em `docs/security/final-security-debt.md` (mesma família do MEDIUM-1 já aceito pelo owner; decisão de aceite = owner, contexto de lab). Escopo infalsificável por identidade exigiria JIT/`X-Entra-OID` no caminho FlowEvents **ou** ingress interno + rota autenticada no gateway — ambos tocam o caminho de identidade → **fora de escopo por AC-9** (retro-compat). `region`/`deployTime` permanecem infalsificáveis (backend/build).

**H-2 (caminho feliz não provado pela topologia) — registrado, sem mudança de código:** via gateway (`{gateway}/flow-events`), o `MapReverseProxy().RequireAuthorization()` blanket (`Gateway/Program.cs:637-639`) devolveria **401** porque o cliente não manda token — padrão herdado do `fetchRecentPurchases`/F6 (Story 2.6 Done). Ou seja: ou o `/flow` também está **latentemente quebrado via gateway**, ou tudo funciona pelo **FQDN direto do `ca-flow`** (anônimo). O Diploma **não introduz** esse comportamento — herda o do F6. **Item de validação E2E** ("Diploma + /flow: decidir/validar URL semeada — gateway vs FQDN direto do `ca-flow`") acrescentado à lista de validação ao vivo da Story 3.3 (Architect Gate, item 5) para a sessão ao vivo resolver.

**L-1 (limitação conhecida das OG tags) — registrado, sem mudança de código:** as OG tags são injetadas via `<Helmet>` (client-side, SPA sem SSR) → **invisíveis ao crawler do LinkedIn**, que não executa JS; além disso `og:image` está como caminho **relativo** (`/og-image.png`), inválido pelo protocolo OG (exige URL absoluta). Consequência: o card do LinkedIn cairá nas OG **genéricas do `index.html`** (sem PII — coerente com AC-6, apenas menos personalizado). O valor pessoal do compartilhamento vai pelo botão **"Copiar resumo"** (texto com a telemetria real), exatamente como a spec da Uma §2.1 desenhou (o `share-offsite` moderno só aceita `url`). Aceitável por design; um card rico exigiria SSR/prerender ou um endpoint de OG dinâmico — fora de escopo.

**Handoff @devops (re-sync do repo real):** o `lab-grande-final.yml` **já foi semeado** no repo real `TFTEC/fifa2026-final-lab` (Story 4.5). As mudanças desta story nesse arquivo (linha do Diploma no summary + `VITE_BUILD_TIME` no build) **precisarão de re-sync** para o repo real — **@devops**, não o @dev.

**Verificação:** FlowEvents `dotnet test` = **24/24** verdes (+2 novos: `DiplomaSummary_returns_scoped_correlationIds_and_count`, `DiplomaSummary_rejects_non_numeric_userId`). Frontend `npm run build` = verde; `eslint` dos arquivos tocados = **0 erros/0 warnings**. Ambos os workflows validados com `js-yaml`.

**Débito frontend-0-testes:** conscientemente **NÃO** resolvido (sancionado pelo épico/story) — nenhum framework de teste de frontend introduzido só para o Diploma. Validação de aceite = dry-run manual com sessão real (Testing standards da story).

**INFO (anti-"correção" futura):** a divergência de copy `"Squad AIOX + você"` (assinatura no card — AC-4, string literal) vs. `"Squad AIOX + eu"` (dentro do texto do "Copiar resumo") é **INTENCIONAL** — o resumo é escrito na 1ª pessoa para o aluno colar como post do LinkedIn (spec da Uma §2.4). **Não corrigir** para "casar" as duas strings.

### File List

**Backend (.NET — FlowEvents, aditivo):**
- `src/Fifa2026.V2.FlowEvents/Data/IFlowEventRepository.cs` — Modificado (novo método `GetPurchasesByUserAsync`)
- `src/Fifa2026.V2.FlowEvents/Data/AppInsightsFlowEventRepository.cs` — Modificado (query `ByUserQuery` + impl + `SanitizeDigits`)
- `src/Fifa2026.V2.FlowEvents/FlowEndpoints.cs` — Modificado (endpoint `GET /api/flow/diploma-summary` + guard `IsValidUserId`)
- `src/Fifa2026.V2.FlowEvents.Tests/FlowEndpointsTests.cs` — Modificado (2 testes novos + `GetPurchasesByUserAsync` no fake)

**Frontend (React — `Lovable/World Cup Tickets Hub/`):**
- `src/pages/Diploma.tsx` — **Novo** (a página do Diploma vivo — spec da Uma)
- `src/App.tsx` — Modificado (rota lazy `/diploma`, aditiva)
- `src/components/layout/Navbar.tsx` — Modificado (link "Diploma" no nav, aditivo)
- `src/lib/flowApi.ts` — Modificado (`fetchDiplomaSummary` + tipo `DiplomaSummary`)
- `src/vite-env.d.ts` — Modificado (tipo `VITE_BUILD_TIME`)

**Workflows (Task 5 / AC-1 + injeção do build-time):**
- `docs/entrega/fifa2026-final-lab/.github/workflows/lab-grande-final.yml` — Modificado (URL do Diploma no summary + `VITE_BUILD_TIME` no build) — **re-sync do repo real fica com @devops**
- `.github/workflows/lab-a-final.yml` — Modificado (`VITE_BUILD_TIME` no build)

**NÃO tocados (retro-compat AC-9):** `AuthProvider.tsx`, `AuthContext.tsx`, `api.ts`, `apiV2.ts`, rotas existentes.

**Emenda MEDIUM-4 (2026-07-03) — arquivos tocados:**

*Gateway (.NET):*
- `src/Fifa2026.V2.Gateway/appsettings.json` — Modificado (rota `flow-events-diploma` antes do catch-all)
- `src/Fifa2026.V2.Gateway/Program.cs` — Modificado (consts `DiplomaRouteId`/`DiplomaKeyHeader` + transform route-scoped `X-Diploma-Key`)
- `src/Fifa2026.V2.Gateway.Tests/GatewayKeyInjectionTests.cs` — Modificado (helper `ForwardedDiplomaKeys` + teste `DiplomaRoute_Injects_XDiplomaKey_ButNever_XGatewayKey` + *rodada 3 — N-1:* `NoToken_OnDiplomaSummary_Returns401`)

*FlowEvents (.NET):*
- `src/Fifa2026.V2.FlowEvents/FlowEndpoints.cs` — Modificado (validação `X-Diploma-Key` timing-safe route-scoped + helper `IsTrustedDiplomaKey` + consts + comentário de postura)
- `src/Fifa2026.V2.FlowEvents/Program.cs` — Modificado *(rodada 2 — MEDIUM-2)* (WARNING de startup quando `DiplomaSharedSecret` vazio em produção)
- `src/Fifa2026.V2.FlowEvents.Tests/FlowEndpointsTests.cs` — Modificado (2 testes novos + consts espelhadas + `using Microsoft.AspNetCore.Hosting`)

*Frontend (React):*
- `Lovable/World Cup Tickets Hub/src/lib/flowApi.ts` — Modificado (`fetchDiplomaSummary` via `VITE_GATEWAY_V2_URL` + Bearer)
- `Lovable/World Cup Tickets Hub/src/pages/Diploma.tsx` — Modificado (comentários de postura + *rodada 2 — MEDIUM-1:* gate na sessão CIAM `useIsAuthenticated`, `userId` resolvido via `meV2()`, estado-gate "Login v2" + *rodada 3 — UX-1:* CTA de login do gate visível em todos os breakpoints via `useMsal().loginPopup`; imports finais `useIsAuthenticated`/`useMsal`/`meV2`/`isEntraConfigured`/`loginRequest`)

*Docs:*
- `docs/runbooks/final-portal-guide.md` — Modificado (Fase 7.4 flip, Fase 9 amarra, Apêndice D)
- `docs/diagrams/final-topologia-seguranca.drawio` — Modificado (edge `X-Diploma-Key` + nota; XML well-formed)
- `docs/security/final-security-debt.md` — Modificado (MEDIUM-4 → FECHADO pela Opção F)
- `docs/architecture/ade-009-network-secrets-service-identity.md` — Emenda v1.1 (já registrada em paralelo pelo @architect)

**NÃO tocados na emenda:** `gatewayKeyClusters` (Program.cs), os 4 endpoints existentes do FlowEvents, ingress `external` do `ca-flow`, `/recent`/timeline/replay/SignalR, `AuthProvider`/`api.ts`/`apiV2.ts`, workflows (`VITE_GATEWAY_V2_URL` já presente). **Re-sync `TFTEC/fifa2026-final-lab` = @devops.**

---

## QA Results

### Gate `@architect` (Aria) — Fidelidade do draw.io à ADE-009 + sanidade do delta FlowEvents · 2026-07-02

**Veredito global: ✅ PASS** (com 2 refinamentos de honestidade **opcionais e não-bloqueantes** para o @analyst). Escopo do gate: AC-7/AC-8/AC-10 (draw.io) + sanidade arquitetural do delta FlowEvents + retro-compat. Não avaliei a experiência (gate `@pm`, abaixo).

#### Dimensão 1 — Fidelidade do `docs/diagrams/final-topologia-seguranca.drawio` (AC-7/8/10): **PASS**

Auditoria componente-a-componente contra as fontes (ADE-009, ADE-010, Stories 4.1/4.2 Done):

| Verificação | Fonte da verdade | Resultado |
|---|---|---|
| `flow-events` **EXCLUÍDO** do X-Gateway-Key (edge "SEM X-Gateway-Key") | `Gateway/Program.cs:93-97` — `gatewayKeyClusters = {backend-v1, functions-f1, mcp-server}`; comentário l.82 "flow-events NÃO está aqui" | ✅ Bate 1:1 com o HashSet real |
| X-Gateway-Key → gateway→Functions/backend-v1/McpServer (3 edges vermelhos) | Mesmo HashSet + ADE-009 Inv 1 | ✅ Exato |
| X-Entra-OID (edges âmbar tracejados; McpServer "só logging, nunca revalida JWT") | ADE-009 Inv 1 / ADE-007; `McpServer/Program.cs:19-22` | ✅ Fiel |
| MI-AAD data-plane; **McpServer `db_datareader`-ONLY** (edge `eMcpSql`) | ADE-009 Inv 2 (regra de ouro) | ✅ Representado corretamente como garantia de RBAC |
| KV references via UA-MI `id-fifa2026-kv-reader` (role Key Vault Secrets User) | ADE-010 (MI+KV); nome esperado | ✅ Nome correto, papel correto |
| **T2 (VNet/PE) = showcase/opcional** — boundary cinza tracejado + tags "OPCIONAL" + edge PE↔SQL condicional | AC-8 + ADE-009 §Priorização | ✅ Marcado em 5 lugares (subtitle, vnetLbl, pe, peTag, legenda) |
| **T3 apenas NOMEADO em callout, não desenhado** | AC-8 / ADE-009 §Priorização | ✅ `tierT3` + `lg9`: "NOMEADO, NÃO CONSTRUÍDO — NÃO desenhado aqui" |
| Convenção de caixas rotuladas p/ produtos sem ícone azure2 (KV/MI/SignalR/PE) | Convenção declarada do `final-f5-f6-mcp-flow.drawio` | ✅ `stencilNote` documenta honestamente; nenhuma iconografia inventada |
| Nenhum componente inventado/aspiracional desenhado como real | Art. IV | ✅ Todos os 15 nós mapeiam a artefato real |

**Refinamentos opcionais (NÃO bloqueiam — nits de honestidade, não falsidades):**
- **R-1 (`ca-mcp` "ingress INTERNO"):** o rótulo afirma ingress interno como fato. Não há **IaC versionado** garantindo isso (ADE-009 §Context 2; débito HIGH-2). A nota do próprio nó já reconhece ("fecha a lacuna 'sem IaC de ingress:internal'") e o provisionamento ao vivo na SUBS-HML de fato tornou o `ca-mcp` interno — então é **fiel ao sistema provisionado**, só não ao repo. Sugestão: parenteticar "(provisionado/alvo — não versionado em IaC)" para o mesmo rigor aplicado ao T2. Opcional.
- **R-2 (plano âmbar MI+KV como T1 construído):** o débito HIGH-3 registra que MI+KV **não** está em IaC versionado (é hands-on via Portal — ADE-010). O subtitle já amarra o plano de segredos a "ADE-010 (MI+KV hands-on Portal)", tornando-o honesto-por-contexto. Sugestão opcional: acrescentar "(provisionado hands-on via Portal — ADE-010)" ao rótulo do `secBoundary`, espelhando a honestidade de escopo do T2. Opcional.

Nenhum dos dois é invenção ou desatualização — o diagrama é consistente com ADE-009/010 e marca T2/T3 honestamente. São nits de precisão versionado-vs-provisionado, não gates.

#### Dimensão 2 — Sanidade do delta FlowEvents (`GET /api/flow/diploma-summary`): **PASS · referendo o débito MEDIUM-4**

- **Coerente com o F6/ADE-008:** reúso da MESMA fonte App Insights do F6 (`ByUserQuery` filtra `customDimensions.UserId`, já gravado pelo `PurchaseEntryFunction`) — nenhuma nova fonte de verdade, nenhuma nova query SQL, nenhuma nova superfície de auth. Aderente ao design F6.
- **Guard anti-injection em profundidade (na verdade 3 camadas):** (1) `IsValidUserId` no endpoint (dígitos-only, ≤12 chars) → 400; (2) `SanitizeDigits` antes de interpolar no `declare`; (3) `query_parameters(userId:string)` parametrizado do Kusto. Sólido — sem superfície de injeção.
- **Postura de exposição descrita HONESTAMENTE nos 3 lugares exigidos:** ✅ Dev Agent Record da 4.6 ("Risco residual — CORRIGIDO após code review; a claim anterior era factualmente falsa"); ✅ comentário `FlowEndpoints.cs:82-87` ("POSTURA honesta, NÃO fail-closed... ANÔNIMO no FQDN externo... Débito registrado"); ✅ `final-security-debt.md` MEDIUM-4. Os três são consistentes e corrigem explicitamente a claim falsa anterior. **Isto é registro exemplar** — o antídoto ao anti-padrão "débito escondido como feito".
- **Referendo o débito (não exijo fix agora):** o fix real (JIT/`X-Entra-OID` no caminho FlowEvents, ou ingress interno + rota autenticada no gateway) **tocaria o caminho de identidade → violaria o AC-9** (retro-compat) desta story. O `/api/flow/recent` (F6, Story 2.6 **Done**) já expõe os MESMOS GUIDs opacos **sem escopo** desde 2026; a resposta tem **zero PII** (region + count + GUIDs opacos). Delta de risco real = só o *vínculo* `userId`→GUIDs sobre dado que já era público. Mesma família do MEDIUM-1 já aceito pelo owner; autoridade de aceite = owner (contexto de lab didático). **Concordo com MEDIUM, não HIGH.**

#### Dimensão 3 — Retro-compat: **PASS**

- **Delta 100% aditivo (confirmado por `git diff HEAD`):** `Gateway/Program.cs` = **ZERO mudanças** (o HashSet que exclui `flow-events` é pré-existente da Story 4.2 — esta story não toca o perímetro). `IFlowEventRepository` = **+10 linhas** (novo método `GetPurchasesByUserAsync`, contrato existente intocado). `FlowEndpoints.cs` = +50 (os 4 endpoints existentes preservados). `AppInsightsFlowEventRepository.cs` = +66. Nenhuma linha removida/alterada nos artefatos existentes.
- **Frontend aditivo:** rota `/diploma` nova; `AuthProvider`/`api.ts`/`apiV2.ts` intocados (File List confirma). Coerente com AC-9.
- **Workflows:** diffs mínimos (`VITE_BUILD_TIME` + linha da URL do Diploma).

#### O que (se algo) bloqueia

**Nada bloqueia.** Gate `@architect` = **PASS**. Os 2 refinamentos (R-1, R-2) são melhorias de honestidade opcionais para o @analyst aplicar quando conveniente — não condicionam o PASS e não exigem re-gate. Checkbox 4.2 marcada. Status da story **inalterado** (aguarda gate `@pm` abaixo).

_— Aria (@architect) · Squad AIOX TFTEC_

---

### Gate @pm (Morgan) — Experiência/Produto (ux-review + experience-validation) — 2026-07-02

**PASS.** O Diploma entrega o clímax pretendido ("a Copa que você construiu te responde"): H1 fiel ao plano §1, correlation-IDs transformados em selos de autenticidade (mono truncado + tooltip + copy, âncora "os IDs nasceram no backend"), e o troféu com peso visual dourado/animações gated por reduced-motion. Copys pt-BR à altura da aula. Loop viral honesto (AC-5): "Copiar resumo" vende a conquista com telemetria real e o toast-dica costura os dois botões numa jornada. Estados convidam à ação — o empty vira convite (comprar/ver o fluxo, assinatura presente) e o não-autenticado explica o porquê. Assinatura "Squad AIOX + você" literal e bem posicionada (AC-4). **Referendo os dois deferimentos** (html2canvas — captura flaky pioraria o "uau"; card genérico do LinkedIn — limitação honesta de SPA, valor pessoal roteado ao resumo copiado) e o **débito MEDIUM-4** de olhos abertos: a promessa "infalsificável" é de integridade-de-origem e permanece garantida; a exposição é leitura de GUIDs opacos não-PII, mesma classe do /flow/recent já aceito. Achados só LOW/fast-follow: og:image relativa→absoluta + confirmar asset (LOW-1); "Baixar imagem" via OG dinâmica pós-lançamento (LOW-2); linha do summary mais celebratória (LOW-3). Nenhum bloqueia o lançamento.

**Resolução dos LOW pelo @dev (mesma rodada):** LOW-1 — `og:image` (`/og-image.png`) **confirmado existente** em `Lovable/World Cup Tickets Hub/public/`; mantida (URL absoluta fica como limitação registrada em L-1, pois o domínio varia por aluno). LOW-3 — linha do Diploma no job summary tornada celebratória. LOW-2 — fast-follow pós-lançamento (fora de escopo).

_— Morgan (@pm) · Squad AIOX TFTEC_

---

### Gate @architect (Aria) — Emenda MEDIUM-4 (Opção F) · 2026-07-03

**Veredito global: ✅ PASS.** Escopo do gate: fidelidade da implementação do @dev ao design que eu mesma desenhei (Opção F) e à ADE-009 v1.1 (emenda route-scoped à Invariante 1) + retro-compat dura + draw.io + sanidade das correções da rodada 2. Evidência coletada por leitura de `git diff HEAD`, verificação na fonte da verdade (não de memória) e **execução real dos testes** (não afirmação do @dev). Status da story **inalterado** (segue **Ready** — Done só na validação ao vivo).

#### Check 1 — Rota + transform route-scoped batem com a Emenda v1.1 (Inv 1): **PASS**

- `appsettings.json`: rota `flow-events-diploma` (path exato `/flow-events/api/flow/diploma-summary`, `Methods: [GET]`, `PathSet → /api/flow/diploma-summary`) casada **antes** do catch-all `flow-events-api`; **sem** `AuthorizationPolicy` explícito → herda o blanket `MapReverseProxy().RequireAuthorization()` (verificado `Program.cs:684-686`). Prova de identidade ✅.
- `Program.cs`: injeção escopada por **`RouteId`** (`if Route?.RouteId == "flow-events-diploma"`, l.247+) — molde exato do `me-get` (Story 3.5), **não** por `ClusterId`. Header **distinto** `X-Diploma-Key`, valor = **mesmo** `adminSharedSecret` (`Gateway:AdminSharedSecret`). Injeção condicional (`!IsNullOrEmpty` → paridade fail-closed/legado da Inv 1(a)). Fiel 1:1 à emenda v1.1 (header route-scoped, não cluster-scoped). ✅

#### Check 2 — FlowEvents valida timing-safe SÓ no diploma-summary: **PASS**

- `FlowEndpoints.cs`: handler `diploma-summary` lê `DiplomaSharedSecret`; não-vazio + `X-Diploma-Key` ausente/divergente → **401** via `IsTrustedDiplomaKey` → `CryptographicOperations.FixedTimeEquals` (timing-safe; false p/ tamanhos diferentes sem lançar; header vazio → false). Segredo vazio → **bypass legado** (dev local + pré-provisionamento). Os outros 4 endpoints (`/recent`, `/{correlationId}`, `/{id}/replay`, Hub) **intocados**. Semântica idêntica ao `GatewayKeyValidator` das Functions (Story 4.2). ✅
- Helper local (não referência ao projeto Functions): reúso exigiria acoplamento novo por 4 linhas — decisão de reúso-vs-acoplamento **arquiteturalmente correta** (evita dependência entre bounded contexts). ✅

#### Check 3 — Retro-compat (dura): **PASS** — com evidência de teste executado

- `gatewayKeyClusters` = `{BackendV1, FunctionsF1, McpServer}` (`Program.cs:100-105`) — `flow-events` **NÃO** está no conjunto. **Intocado**. ✅
- **Testes executados nesta máquina (dotnet 10.0.301):** `FlowEvents 26/26` · `Gateway 41/41`. Os dois testes-âncora rodados isolados e **verdes**: `FlowEvents_Cluster_Does_NOT_Receive_GatewayKey` (o cluster segue fora do `X-Gateway-Key`) + `DiplomaRoute_Injects_XDiplomaKey_ButNever_XGatewayKey` (rota injeta `X-Diploma-Key`, descarta o forjado, e `X-Gateway-Key` **nunca vaza** ao cluster flow-events). ✅
- Ingress do `ca-flow` segue `external` (sem flip de rede, blast-radius zero no F6); `AuthProvider`/`api.ts`/`apiV2.ts` intocados (só `flowApi.ts`/`Diploma.tsx` mudam no front). F6 (`/recent`/timeline/replay/SignalR) sem mudança de comportamento. ✅

#### Check 4 — draw.io (`final-topologia-seguranca.drawio`): **PASS**

- Edge `eGwFlowDiploma` (`gw → flow`, tracejado, "`X-Diploma-Key` (só rota /diploma-summary, Bearer-gated)") **adicionado**; nota `diplomaNote` explicando "Diploma = único endpoint do FlowEvents atrás de auth+proveniência"; edge existente `eGwFlow` re-rotulado "recent/timeline/WebSocket — SEM X-Gateway-Key" (mantém a verdade do MEDIUM-1). Topologia continua fiel (nenhum componente inventado; o cluster segue fora do `X-Gateway-Key`, coerente com o código). ✅
- **XML revalidado well-formed** via `.NET [xml]` (PowerShell): root `mxfile`, 86 `mxCell`, o edge e a nota presentes (1 cada). ✅

#### Check 5 — Correções da rodada 2 são arquiteturalmente sãs: **PASS**

- **Gate CIAM reusando `meV2()`** (`Diploma.tsx`): resolve o `userId` pela MESMA fonte que o Checkout (Story 3.5), casando com `customDimensions.UserId` da telemetria (int do `/me`, não o id v1) e cobrindo o aluno nato-CIAM. Corrige o split de identidade v1↔CIAM sem tocar `AuthProvider`. Não contradiz a ADE — **reforça** a coerência "identidade base v1↔CIAM" da Final. ✅
- **`LogWarning` de segredo vazio em produção** (`FlowEvents/Program.cs`): fail-LOUD (não derruba o serviço, preserva o pré-provisionamento) e **operator-facing** (não exposto no `/health` público → não sinaliza a um atacante endpoint aberto). Escolha correta dado que a Inv 1 é intencionalmente permissiva com segredo vazio; adiciona a visibilidade que o owner exigiu ("débito ≠ silêncio"). ✅
- **Strip GLOBAL do `X-Diploma-Key`** (`Program.cs` transform global): defesa em profundidade — o cliente não forja o header em NENHUMA rota; a rota diploma reinjeta o valor real depois (ordem de registro dos request transforms garante strip-antes-de-injetar). Consistente com o tratamento dos headers `X-Entra-*`. Não explorável hoje, fecha o gap por consistência. ✅

#### Check 6 — Nota de produto (`/diploma` passa a exigir Login v2/CIAM): **ACEITO arquiteturalmente**

Corolário direto da Opção F: como o Bearer de cliente é token CIAM, gatear na sessão CIAM (`useIsAuthenticated`) é a consequência necessária. **É coerente com a identidade da Grande Final** — a compra que gera a telemetria do Diploma já usa CIAM (Story 3.5 JIT `/api/v2/me`); exigir a mesma identidade para *ver* a prova dessa compra é consistente, não uma regressão. Registro como **ACEITO** na dimensão arquitetural. A única decisão que sobra é de **produto** (se deveria existir um caminho de Diploma para aluno só-v1) — já sinalizada pelo @dev como "consequência reportada, não decidida" no Change Log 0.7; encaminho ao **@pm** como ponto de produto, **não** bloqueante deste gate (na Final o aluno é CIAM por design).

#### O que (se algo) bloqueia

**Nada bloqueia.** Gate `@architect` da Emenda MEDIUM-4 = **PASS**. Implementação fiel ao design da Opção F e à ADE-009 v1.1; retro-compat provada por teste executado; draw.io fiel e well-formed; correções da rodada 2 sãs. Uma ressalva **não-bloqueante** (ponto de produto ao @pm): confirmar que exigir CIAM no `/diploma` é o comportamento desejado para a turma. Pendências que seguem abertas (fora do meu gate): code review adversarial (Task M4-5.1), gate `@qa` (M4-5.2), re-sync `TFTEC/fifa2026-final-lab` (@devops), e a validação ao vivo (item 5 da Story 3.3). Status **inalterado (Ready)**.

_— Aria (@architect) · Squad AIOX TFTEC_

---

### Gate `@qa` (Quinn) — Emenda MEDIUM-4 (Opção F) · 2026-07-03

**Veredito global: ✅ PASS.** Escopo deste gate: qualidade dos testes, cobertura dos `AC-M4-1..7`, regressão e OWASP básica sobre o **delta** da emenda (não re-avalio fidelidade ADE/design — gate `@architect` acima; nem experiência — gate `@pm`). Meu ângulo: **os testes provam o que dizem, os ACs estão cobertos, não há regressão**. Testes rodados de fato na worktree `story-3.2-n8n-decommission` (HEAD `63cebbd`): **FlowEvents 26/26 · Gateway 41/41** (verde, reproduzido — não afirmação do @dev). Nada bloqueia. 4 notas LOW/INFO não-bloqueantes.

#### Check 1 — Os testes provam o que alegam (resistência a mutação): **PASS**

Auditei se um bypass **quebrado** ainda passaria — os 3 testes novos são genuinamente mutation-resistant, não decorativos:

| Teste | O que o quebraria (mutação) | Genuíno? |
|---|---|---|
| `DiplomaSummary_rejects_missing_or_wrong_key_when_secret_set` (FlowEvents) | Segredo armado + header **ausente E divergente** → asserta **401** nos dois. Guard removido/inerte (→200) ou compare always-true → **falha**. | ✅ Prova fail-closed real |
| `DiplomaSummary_with_correct_key_returns_200_when_secret_set` (FlowEvents) | Key correta → 200 + `count==2`. Validação divergente do valor real → **falha**. | ✅ Prova o caminho feliz |
| `DiplomaRoute_Injects_XDiplomaKey_ButNever_XGatewayKey` (Gateway) | Cliente **forja** `X-Diploma-Key=forged-evil-key`; asserta segredo real forwardeado, forjado **NÃO** (`DoesNotContain`), `X-Gateway-Key` **nunca** vaza (`Empty`). Anti-spoof `Remove` sumindo → forjado passa → **falha**; injeção route-scoped sumindo → `Contains(AdminSharedSecret)` → **falha**. | ✅ Injeção + anti-spoof + isolamento de cluster |

Contagens confirmadas por execução real (`dotnet test`): FlowEvents **26/26** (24→26), Gateway **41/41** (40→41). Um bypass inerte **falharia** — não passa silenciosamente.

#### Check 2 — Cobertura dos ACs (`AC-M4-1..7`): **PASS** (1 gap de teste, sancionado)

| AC | Evidência | Cobertura |
|---|---|---|
| **AC-M4-1** (front Bearer via `VITE_GATEWAY_V2_URL`) | `flowApi.ts` + `Diploma.tsx` · build verde + eslint 0/0 | Código; **sem teste automatizado** → N-2 |
| **AC-M4-2** (rota + transform route-scoped) | `appsettings.json`+`Program.cs` · teste `DiplomaRoute_Injects...` | Código **+ teste** |
| **AC-M4-3** (validação timing-safe; vazio→bypass) | `FlowEndpoints.cs`/`IsTrustedDiplomaKey` · 2 testes novos (armado) + 2 legados (bypass vazio) | Código **+ teste** (ambos ramos) |
| **AC-M4-4** (guia Fase 7.4/9/Apêndice D) | `final-portal-guide.md`: secret KV-ref `diploma-shared-secret`, App Setting `DiplomaSharedSecret`, WARNING de verificação, troubleshooting 401 | Doc verificado |
| **AC-M4-5** (draw.io edge `X-Diploma-Key`) | `eGwFlowDiploma` + `diplomaNote`; **XML well-formed** (revalidei `[xml]`) | Doc verificado |
| **AC-M4-6** (retro-compat — bloqueante) | `FlowEvents_Cluster_Does_NOT_Receive_GatewayKey` **presente e verde** (nos 41/41) — ver Check 3 | **Teste** |
| **AC-M4-7** (fecho honesto) | `final-security-debt.md` MEDIUM-4 → **FECHADO**; comentários de postura em `FlowEndpoints.cs`/`Diploma.tsx` migrados "anônimo"→"protegido"; residual + Opção D como fast-follow | Doc/código verificado |

Todos os 7 ACs têm evidência. **Único gap de cobertura de teste** = AC-M4-1 (frontend) — coberto por build/eslint + dry-run ao vivo; débito frontend-0-testes já sancionado pelo épico/story (N-2), não gap silencioso.

#### Check 3 — Regressão: **PASS**

- **Bypass legado (retro-compat) ainda coberto:** os 2 testes pré-existentes (`DiplomaSummary_returns_scoped_correlationIds_and_count` 200 / `DiplomaSummary_rejects_non_numeric_userId` 400) rodam com `DiplomaSharedSecret` **vazio** → provam que o comportamento pré-emenda (200/400) segue intacto quando o segredo não está armado. A mudança de assinatura do handler (`+HttpRequest`) não os quebrou (26/26).
- **F6 intocado:** `FlowEvents_Cluster_Does_NOT_Receive_GatewayKey` presente (grep=1) e verde — cluster `flow-events` segue **fora** do `X-Gateway-Key`; injeção é por **rota**, não por cluster.
- **Frontend `/recent`/timeline/replay/hub:** confirmado no diff — **só** `fetchDiplomaSummary` mudou de base; `FLOW_BASE` preservada para os demais. `AuthProvider`/`api.ts`/`apiV2.ts` intocados.
- **Ingress `external` do `ca-flow`:** sem flip de rede (blast-radius zero no F6).

#### Check 4 — Segurança OWASP básica: **PASS**

- **Timing-safe correto:** `CryptographicOperations.FixedTimeEquals` sobre bytes UTF-8; `IsTrustedDiplomaKey` retorna `false` para header nulo/vazio (fail-closed quando o segredo está armado). Família do `GatewayKeyValidator` (Story 4.2). *(INFO N-4: `FixedTimeEquals` retorna cedo em tamanhos divergentes → vaza o comprimento por timing; irrelevante para segredo provisionado de alta entropia; paridade com o padrão já aceito.)*
- **Segredo não vaza:** o único log no caminho é `LogInformation("Resumo do Diploma: {Count} compra(s)…")` — **sem** segredo/header. O `LogWarning` de startup (MEDIUM-2) imprime **só** o nome do ambiente + instrução — **não** o valor do `DiplomaSharedSecret` (verificado no diff). Header injetado **server-side** pelo gateway → **não** vai ao bundle (front só manda o Bearer). Grep de logs confirmou zero impressão de secret/key.
- **Fail-closed quando armado + header ausente:** `Results.Unauthorized()` (corpo vazio) — provado por teste.
- **Anti-spoof:** `X-Diploma-Key` do cliente removido **globalmente** (junto de X-Entra-OID/Email/Name, incondicional por request) **e** reinjetado route-scoped; teste prova que o forjado não alcança o backend.

#### Check 5 — Edge cases do frontend: **PASS**

`gateReady = isEntraConfigured() && useIsAuthenticated()` cobre os 3 caminhos sem loading infinito: (a) **nato-CIAM** logado → `queryFn` resolve `userId` via `meV2()` (casa com `customDimensions.UserId`); (b) **só-v1**/não-logado → `GateCard` "Login v2" (popup **iniciado pelo aluno**, nunca no load; `getV2AccessToken` só dispara com sessão); (c) **CIAM não configurado** → "Voltar ao login". O `authLoading ? skeleton` antigo foi trocado pelo gate imediato — sem spinner solto. `enabled: gateReady` → nenhum fetch anônimo → nenhum 401 surpresa. `meV2()` que falha lança com mensagem → estado de erro do react-query (não trava).

#### Check 6 — Performance: **N/A material · PASS**

1 chamada extra `meV2()` na `queryFn` (leve, cacheada por `staleTime: 60s`) — a MESMA resolução que o Checkout já faz. Sem impacto material.

#### Check 7 — Docs coerentes com o código: **PASS**

Guia (Fase 7.4 flip para `DiplomaSharedSecret` como KV-ref ao **mesmo** `gateway-admin-shared-secret`; Fase 9 "um segredo, cinco consumidores"; Apêndice D troubleshooting `/diploma` 401) e `final-security-debt.md` (MEDIUM-4 → **FECHADO pela Opção F**, item 3 do plano de remediação reconciliado) batem 1:1 com `Program.cs`/`FlowEndpoints.cs`/`flowApi.ts`. Sem divergência doc↔código.

#### Notas não-bloqueantes (LOW/INFO — nenhuma condiciona o PASS)

- **N-1 (LOW):** sem teste dedicado do 401 **sem Bearer** na rota `flow-events-diploma` (a prova de identidade herda o blanket `RequireAuthorization` existente — comportamento não-novo). Um teste de 1 linha o solidificaria; opcional.
- **N-2 (INFO):** AC-M4-1 (front Bearer) sem teste automatizado — débito frontend-0-testes sancionado; validado por build/eslint + dry-run ao vivo.
- **N-3 (PRODUTO, não-QA):** só-v1 puro não vê o Diploma (corolário da Opção F) — já sinalizado pelo `@dev` (Change Log 0.7) e encaminhado ao `@pm` pelo `@architect` (Check 6 do gate acima). Confirmar ciência do owner; não bloqueia.
- **N-4 (INFO):** `FixedTimeEquals` vaza length-mismatch por timing — aceito (paridade com `GatewayKeyValidator`, segredo de alta entropia).

#### O que (se algo) bloqueia

**Nada bloqueia.** Gate `@qa` da Emenda MEDIUM-4 = **PASS**. Testes provam a proteção (mutation-resistant), 26/26 + 41/41 reproduzidos nesta máquina, retro-compat verde, sem vazamento de segredo, docs coerentes. Status da story **inalterado (Ready)** — Done fica com a validação ao vivo. Pendências fora do meu gate: re-sync `TFTEC/fifa2026-final-lab` (@devops) e a validação E2E ao vivo (item 5 da Story 3.3).

_— Quinn (@qa) · Squad AIOX TFTEC_

---

## Emenda MEDIUM-4 — Proteção do `diploma-summary` (aprovada pelo owner 2026-07-03)

> **Status da emenda:** ✅ Design aprovado pelo owner (2026-07-03) · pronta para `@dev` · **ADE-009 v1.1** (exceção route-scoped à Inv 1) já registrada.
> **Executor:** `@dev` (Dex) — gateway + FlowEvents + frontend + testes · **Gate:** `@architect` (fidelidade à ADE-009 v1.1 + retro-compat) + `@qa` (retro-compat F6). **Re-sync `TFTEC/fifa2026-final-lab` = @devops.**
> **Tamanho:** **M** (cada mudança é pequena, mas cruza gateway + FlowEvents + frontend + guia + draw.io + re-sync).

### Contexto (por que esta emenda existe)

A implementação original da 4.6 registrou o `GET /api/flow/diploma-summary?userId={int}` como **débito MEDIUM-4** (`docs/security/final-security-debt.md`): como o cluster `flow-events` está fora do `X-Gateway-Key` (ADE-009 Inv 1) e o `ca-flow` tem ingress **externo**, o endpoint é **anônimo na internet, com `userId` sequencial enumerável** (resposta zero-PII: só `region` + GUIDs opacos + count). **O owner REJEITOU aceitar o débito (2026-07-03)** e pediu **proteção real** (implementar, não registrar — coerente com o feedback "débito ≠ entrega"). O `@architect` mapeou 6 opções (A–F); o owner **aprovou a Opção F**.

### O design aprovado — Opção F (defesa em profundidade)

Duas provas **independentes**, ambas que um chamador anônimo da internet não consegue satisfazer:

1. **Prova de identidade** — o frontend passa a mandar `Authorization: Bearer` na chamada do Diploma. O blanket `MapReverseProxy().RequireAuthorization()` do gateway (`Gateway/Program.cs:637-639`) barra qualquer anônimo **via gateway** (sem token → 401).
2. **Prova de proveniência** — o gateway injeta `X-Diploma-Key` (reuso do **mesmo** valor `Gateway:AdminSharedSecret`/`GATEWAY_SHARED_SECRET` da Fase 9, sob header **distinto**) **apenas na rota** `diploma-summary` (escopo por `RouteId`, molde do `me-get`). O FlowEvents valida a key **só** nesse endpoint → barra o **FQDN direto** do `ca-flow` (o atacante não conhece o segredo).

**Invariantes preservadas (retro-compat = requisito):**
- **Ingress do `ca-flow` fica `external`** — **sem flip de rede**, blast-radius **zero** no F6 "Done".
- `/api/flow/recent`, `/{correlationId}`, `/{id}/replay` e o **Hub SignalR** ficam **intocados** (seguem como MEDIUM-1 aceito).
- O cluster `flow-events` **continua fora** do `X-Gateway-Key` — o teste **`FlowEvents_Cluster_Does_NOT_Receive_GatewayKey` DEVE seguir verde** (header/mecanismo distintos; injeção por rota, não por cluster).
- Reuso do segredo já ensinado (Story 4.2 + Fase 9 MI+KV) → **US$0**, atrito didático mínimo.

> **Bônus (conserta bug latente H-2):** hoje `fetchDiplomaSummary` **não** manda Bearer → via gateway o `/diploma` já daria **401** (item 5 da validação ao vivo da Story 3.3). Passar a mandar o Bearer **conserta** esse caminho — o `/diploma` passa a funcionar via gateway como deveria.

### Acceptance Criteria da emenda (`AC-M4-*`)

- **AC-M4-1** — `fetchDiplomaSummary` (`Lovable/World Cup Tickets Hub/src/lib/flowApi.ts:88-97`) chama via **`VITE_GATEWAY_V2_URL`** (a mesma base do `apiV2.ts`, **não** a `FLOW_BASE` genérica de `/recent`) e envia `Authorization: Bearer ${await getV2AccessToken()}` (accessor `@/lib/authV2`, já usado por `apiV2.ts:12/58`). Anônimo via gateway → **401**. *(Fonte: design Opção F, prova de identidade; padrão Bearer de `apiV2.ts`.)*
- **AC-M4-2** — Gateway: **nova rota** `flow-events-diploma` (path-específico `/flow-events/api/flow/diploma-summary`, casada **antes** do catch-all `flow-events-api`; permanece sob a `DefaultPolicy`, exige Bearer) + **transform route-scoped** (`if RouteId == "flow-events-diploma"`, molde do bloco `me-get` em `Program.cs:195-235`) que faz **anti-spoof** (`Remove(X-Diploma-Key)`) e injeta `X-Diploma-Key = adminSharedSecret` **só quando não-vazio** (paridade com a semântica da l.254). *(Fonte: ADE-009 v1.1; padrão route-scoped da Story 3.5.)*
- **AC-M4-3** — FlowEvents (`src/Fifa2026.V2.FlowEvents/FlowEndpoints.cs:88-112`): o handler `diploma-summary` lê `configuration["DiplomaSharedSecret"]`; se **não-vazio**, exige `X-Diploma-Key` em igualdade **timing-safe** (`CryptographicOperations.FixedTimeEquals`) → ausente/divergente = **401**; se **vazio → legacy-bypass** (dev local + estado pré-provisionamento — mesma filosofia do `Functions/Infrastructure/GatewayKeyValidator.cs:51-54`). Os outros 4 endpoints **não mudam**. *(Fonte: ADE-009 Inv 1(a) semântica fail-closed/legado, aplicada route-scoped.)*
- **AC-M4-4** — Guia de 14 fases (`docs/runbooks/final-portal-guide.md`): **Fase 7.4** (l.392) — o box "🔒 **não** configure `GATEWAY_SHARED_SECRET` no FlowEvents" **flipa**: agora **configure** `DiplomaSharedSecret` no `ca-flow` como **Key Vault reference** para o **mesmo** secret da Fase 9 (a MI `id-fifa2026-kv-reader` já está anexada ao `ca-flow` pela Fase 7.2 — infra pronta); **Fase 9** — 1 linha amarrando "o mesmo segredo alimenta o `X-Diploma-Key` do `ca-flow`"; **Apêndice D** — nova linha de troubleshooting ("`/diploma` 401 → `DiplomaSharedSecret` ausente no `ca-flow` **ou** front sem Bearer / `VITE_GATEWAY_V2_URL`"). *(Fonte: design Opção F; Fase 9 já arma o segredo via MI+KV.)*
- **AC-M4-5** — Draw.io (`docs/diagrams/final-topologia-seguranca.drawio`): o edge "flow-events: **SEM X-Gateway-Key**" **permanece** (verdadeiro p/ recent/timeline/SignalR); **adicionar** um edge `gateway → ca-flow` rotulado **"`X-Diploma-Key` (só rota `diploma-summary`, Bearer-gated)"**, distinto dos 3 edges vermelhos de `X-Gateway-Key`, + nota "Diploma = único endpoint do FlowEvents atrás de auth+proveniência". XML revalidado well-formed. *(Fonte: ADE-009 v1.1; gate `@architect` de fidelidade.)*
- **AC-M4-6 — Retro-compat (bloqueante):** `/recent`/`/{correlationId}`/`/replay`/SignalR, o cluster `flow-events` (X-Gateway-Key) e o **ingress `external`** do `ca-flow` **inalterados**; o teste **`FlowEvents_Cluster_Does_NOT_Receive_GatewayKey` segue verde**; `AuthProvider`/`api.ts`/`apiV2.ts` intocados (só `flowApi.ts`/`Diploma.tsx` mudam no front). *(Fonte: ADE-009 v1.1 "o que a emenda NÃO altera".)*
- **AC-M4-7 — Fecho honesto:** `docs/security/final-security-debt.md` MEDIUM-4 muda de "aceito" para **"fechado pela Opção F"**; o comentário `FlowEndpoints.cs:82-87` e o Dev Agent Record da 4.6 (o parágrafo "Risco residual") são atualizados de "anônimo/débito" para "protegido (Bearer+X-Diploma-Key)"; o **residual** (enumeração **autenticada** intra-lab, zero-PII) e o **upgrade Opção D** (gateway injeta `X-Entra-OID`; FlowEvents resolve `oid→userId` — **condicionado a verificar** se a telemetria já carrega o `oid`; hoje `customDimensions.UserId` é o int v1, **não assumir** — Art. IV) ficam declarados como **fast-follow**. *(Fonte: postura de registro honesto já elogiada no gate `@architect` da 4.6.)*

### Tasks / Subtasks da emenda

- [x] **Task M4-1 — Gateway (route config + transform) (AC: M4-2, M4-6)**
  - [x] M4-1.1 `src/Fifa2026.V2.Gateway/appsettings.json` — rota `flow-events-diploma` (path `/flow-events/api/flow/diploma-summary`, `PathSet` → `/api/flow/diploma-summary`, `Methods: [GET]`) adicionada **antes** do `flow-events-api`; sob `DefaultPolicy` (sem `AuthorizationPolicy` explícito → herda o blanket `RequireAuthorization`). *(Nota: NÃO foi possível anotar a rota com uma chave `"// …"` dentro de `ReverseProxy:Routes` — YARP interpreta toda chave sob `Routes` como uma rota e falha no startup; a documentação foi para o `Program.cs`.)*
  - [x] M4-1.2 `src/Fifa2026.V2.Gateway/Program.cs` — consts `DiplomaRouteId`/`DiplomaKeyHeader` + bloco `if (Route?.RouteId == "flow-events-diploma")` espelhando o `me-get`: `Remove(X-Diploma-Key)` (anti-spoof) + injeção condicional do `adminSharedSecret`. `gatewayKeyClusters` **intocado** (`flow-events` segue fora).
  - [x] M4-1.3 **(review LOW — defesa em profundidade)** `Program.cs` — `X-Diploma-Key` adicionado ao **strip anti-spoof GLOBAL** (junto de X-Entra-OID/Email/Name): o cliente não forja o header em NENHUMA rota; a rota diploma reinjeta o valor real logo depois. Não explorável hoje, fecha o gap por consistência.
- [x] **Task M4-2 — FlowEvents (validação route-scoped) (AC: M4-3, M4-6)**
  - [x] M4-2.1 `src/Fifa2026.V2.FlowEvents/FlowEndpoints.cs` — handler `diploma-summary` recebe `HttpRequest`, lê `DiplomaSharedSecret`; se setado, exige `X-Diploma-Key` timing-safe (`CryptographicOperations.FixedTimeEquals`) → 401; vazio → bypass legado. Helper `IsTrustedDiplomaKey` + consts. Comentário de postura reescrito (anônimo → protegido).
  - [x] M4-2.2 Testes (`FlowEndpointsTests.cs`): `DiplomaSummary_with_correct_key_returns_200_when_secret_set` (200) + `DiplomaSummary_rejects_missing_or_wrong_key_when_secret_set` (401 p/ ausente E divergente). O bypass-com-segredo-vazio é coberto pelos 2 testes pré-existentes (rodam sem segredo). **FlowEvents 26/26 verdes** (24 → 26).
  - [x] M4-2.3 Nenhuma mudança tocou o cluster `flow-events` no gateway → `FlowEvents_Cluster_Does_NOT_Receive_GatewayKey` **segue verde**. **Extra:** adicionado `DiplomaRoute_Injects_XDiplomaKey_ButNever_XGatewayKey` provando (a) X-Diploma-Key injetado na rota + anti-spoof, (b) X-Gateway-Key nunca vaza. **Gateway 41/41 verdes** (40 → 41).
  - [x] M4-2.4 **(review MEDIUM-2 — sinal de segredo esquecível)** `src/Fifa2026.V2.FlowEvents/Program.cs` — WARNING de startup quando `DiplomaSharedSecret` vazio em ambiente **não-Development** (o bypass legado reabriria o MEDIUM-4 sem sinal). Fail-LOUD, não fail-closed (não derruba o serviço; dev local vazio = silencioso). **Não** exposto no `/health` público (evita sinalizar a atacante). Verificação registrada no guia Fase 7.4.
- [x] **Task M4-3 — Frontend (Bearer + base do gateway) (AC: M4-1, M4-6)**
  - [x] M4-3.1 `src/lib/flowApi.ts` — `fetchDiplomaSummary` usa `VITE_GATEWAY_V2_URL` + `Authorization: Bearer` (`getV2AccessToken`), path `/flow-events/api/flow/diploma-summary`. `/recent`/timeline/replay/hub **não mudam** (seguem na `FLOW_BASE`).
  - [x] M4-3.2 `src/pages/Diploma.tsx` — comentário de POSTURA REAL reescrito (anônimo → protegido Bearer+X-Diploma-Key) + comentário do `enabled`. `AuthProvider`/`api.ts`/`apiV2.ts` intocados. **Build verde; eslint 0/0.**
  - [x] M4-3.4 **(review MEDIUM-1 — split de identidade v1↔CIAM, PRIORIDADE)** `src/pages/Diploma.tsx` — o gate `enabled` passou a exigir a **sessão CIAM** (`useIsAuthenticated` do `@azure/msal-react` + `isEntraConfigured`), não o auth v1; e o `userId` da query é **resolvido via `meV2()`** (mesma resolução Story 3.5 que o Checkout usa) — casa com o `customDimensions.UserId` da telemetria (int do `/me`, não o id v1) e cobre o aluno **nato-CIAM** (sem conta v1). Estado-gate reescrito para "Login v2 (CIAM)" (botão `LoginV2Button`, popup **iniciado pelo aluno** — nunca automático). Conserta: aluno só-v1 → 401 + popup surpresa; e o mismatch userId↔telemetria. **Não quebra** quem já é CIAM. **Ver nota de produto no Completion Notes.**
  - [x] M4-3.3 `VITE_GATEWAY_V2_URL` **já presente** no build do front (`lab-a-final.yml` l.465) — sem mudança/duplicação de var. **Re-sync do repo real `TFTEC/fifa2026-final-lab` = @devops.**
- [x] **Task M4-4 — Guia + Draw.io + débito (AC: M4-4, M4-5, M4-7)**
  - [x] M4-4.1 `docs/runbooks/final-portal-guide.md` — Fase 7.4 (flip: secret `diploma-shared-secret` KV ref + App Setting `DiplomaSharedSecret` + nota de escopo reescrita), Fase 9 (amarra "um segredo, cinco consumidores"), Apêndice D (troubleshooting `/diploma` 401).
  - [x] M4-4.2 `docs/diagrams/final-topologia-seguranca.drawio` — edge `eGwFlowDiploma` (`X-Diploma-Key` route-scoped, Bearer-gated) + nota `diplomaNote`; edge existente `eGwFlow` rotulado "recent/timeline/WebSocket". **XML revalidado well-formed (PowerShell `[xml]`).**
  - [x] M4-4.3 `docs/security/final-security-debt.md` — MEDIUM-4 "aceito" → **"FECHADO pela Opção F"** + residual/Opção D como fast-follow; item 3 do plano priorizado ajustado.
- [x] **Task M4-5 — Gates**
  - [x] M4-5.1 Code review adversarial (reviewer nativo — CodeRabbit sem seat): **APROVADO C/ RESSALVAS** em 2 rodadas — rodada 2 (MEDIUM-1/MEDIUM-2/LOW) e rodada 3 (UX-1/N-1), todas fechadas. Nenhum bypass do key gate; retro-compat F6; segredo nunca em log/bundle.
  - [x] M4-5.2 Gate `@architect` = **PASS (6/6)** + gate `@qa` = **PASS (7/7)** (fidelidade ADE-009 v1.1 + draw.io + retro-compat F6 + testes verdes).
  - [x] M4-5.3 **(review rodada 3 — UX-1)** `src/pages/Diploma.tsx` — CTA de login do GATE agora VISÍVEL em todos os breakpoints (o `LoginV2Button` da navbar é `hidden lg:flex` → sumia no mobile). Botão próprio do gate reusa o MESMO fluxo (`msalInstance.loginPopup(loginRequest)`); navbar intocada.
  - [x] M4-5.4 **(review rodada 3 — N-1)** `src/Fifa2026.V2.Gateway.Tests/GatewayKeyInjectionTests.cs` — teste-âncora `NoToken_OnDiplomaSummary_Returns401` (falharia se a rota virasse anônima). **Gateway 42/42.**

### Residual aceito (declarado, não escondido)

Com a Opção F, **sobra**: um aluno **autenticado** pode passar `?userId=<outro>` via gateway (Bearer válido → o gateway injeta o key) e receber **GUIDs opacos + count** de outro aluno — **zero PII**, igual-ou-menor que o `/recent` (MEDIUM-1, já aceito). Isso é **drasticamente menor** que a objeção original ("anônimo na internet"). O escopo **infalsificável-por-identidade** (aluno A nunca lê aluno B) = **Opção D** (gateway injeta `X-Entra-OID`; FlowEvents resolve `oid→userId`) — só vale se, **verificado**, a telemetria já carregar o `oid` (hoje `customDimensions.UserId` é o int v1; assumir seria invenção — Art. IV). **Fast-follow**, fora desta emenda.
