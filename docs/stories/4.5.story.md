# Story 4.5 — Entrega (Capstone): Template Repository `fifa2026-final-lab` + `lab.config.json` (variáveis 15→1) + `lab-grande-final.yml` (`acao=check` Doctor) + runbook browser-harness

> **Epic:** [EPIC-004 — "Nível Produção"](../epics/EPIC-004-nivel-producao.md)
> **Status:** Ready
> **Created:** 2026-07-01 by @sm (River)
> **Sprint context:** Story do **ponto de vista do aluno** (a experiência de entrega da Grande Final) — construída pelo squad (`@devops`/`@dev`/`@analyst`). É o **ponto de integração** do épico: só fecha com o núcleo Blindar (4.1/4.2, Key Vault + Managed Identity prontos) **e** as features do EPIC-003 disponíveis (é o repositório que empacota a Final inteira). Repositório **novo** — `fifa2026-final-lab` — não é branch deste repositório de trabalho.
> **Depends on:** Story 4.1 (Key Vault + Managed Identity — a entrega os reusa, Inv 2/3) · Story 4.4 (o checklist dos 5 gotchas, AC-11, é insumo direto do Doctor `acao=check`) · EPIC-003 (features da Final — chatbot F5/3.3, visualizer F6/3.4 — precisam estar disponíveis para o repo empacotar a experiência completa; núcleo desta story — o Template Repo + `lab.config.json` + workflow — pode começar em paralelo)
> **Blocks:** Story 4.6 (o Diploma vivo e o draw.io são "surfaced" no Template Repo criado aqui)
> **Source (Art. IV — rastreabilidade):** EPIC-004 §Escopo Frente C + §Decisão estrutural + tabela de Stories (linha S5) · plano `giggly-mapping-hearth.md` §6 (Modelo de entrega) · [ADE-009](../architecture/ade-009-network-secrets-service-identity.md) Inv 2/3 (a entrega materializa essas invariantes para o aluno) · `docs/runbooks/quartas-f2-portal-guide.md` (Fase 5 — tabela real de 15 vars + 5 secrets, molde a colapsar) · molde de formato: [Story 2.11](2.11.story.md)
>
> **Nota de numeração (@sm AUTO-DECISION):** Quinta story da série 4.x.

---

## Executor Assignment

```yaml
executor: "@devops"
quality_gate: "@architect"
quality_gate_tools:
  - "infra-review"
  - "security-validation"
  - "ux-review"
po_gate: "@po"                     # UX do aluno: 1 campo + 1 secret — validação de experiência
dev_support: "@dev"                # schema/resolução de lab.config.json
analyst_support: "@analyst"        # runbook browser-harness (narrativo)
```

**Rationale:** Story de infraestrutura de entrega (Template Repository, workflow CI/CD, runbook de provisionamento) — autoridade `@devops` (criação de repositório, CI/CD é exclusivo `@devops` per `agent-authority.md`). `@dev` implementa o **schema** e a **resolução in-run** de `lab.config.json` (é código de workflow/scripting, não infraestrutura pura). `@analyst` (Atlas) escreve o runbook narrativo do `browser-harness` para o instrutor. `@architect` é o gate de fidelidade às Invariantes 2/3 da ADE-009 (o `lab.config.json`+KV+MI honram "zero segredo no GitHub"; o workflow honra "zero `az create`" e a ordem de rede). `@po` (Pax) valida especificamente a **experiência do aluno** (1 campo, 1 secret — é a promessa central desta story).

---

## Story

**Como** aluno da Grande Final (que já completou Oitavas e Quartas — tem sua própria infraestrutura Azure e identidade CIAM/admin configuradas),
**eu quero** clicar "Use this template" para criar meu **próprio** repositório (não um fork) a partir de `fifa2026-final-lab`, editar **uma única linha** (`sufixo`) em `lab.config.json`, configurar **um único secret** (`AZURE_CREDENTIALS`) e rodar `acao=check` para confirmar que meu ambiente está pronto antes de `acao=tudo`,
**para que** eu não precise preencher as **~15 variáveis + 5 secrets** que as Quartas exigiam nem herde a classe de bugs de merge de fork que atrapalhou turmas anteriores — e possa confiar, com um pré-flight automatizado, que meu ambiente está correto antes de começar os labs de verdade (Blindar/F5/F6/Diploma).

---

## Acceptance Criteria

> **Rastreabilidade (Art. IV):** cada AC cita a fonte do épico/plano ou o artefato real (Quartas) que está sendo colapsado/reusado. Nenhum AC foi inventado.

### Bloco A — Template Repository (mata a classe de bugs de merge de fork)

1. **AC-1 — Repositório novo `fifa2026-final-lab` criado na org TFTEC como GitHub Template Repository** (configuração nativa do GitHub "Template repository", **não** um repositório comum a ser "forkado"): "Use this template" gera um repositório **próprio e independente** para cada aluno (não um fork com histórico compartilhado) — elimina estruturalmente a classe de bugs de merge de aluno que reincidiu nas Oitavas/Quartas (registrado em memória de projeto: causa raiz era `default_repository_permission: write` da org, dando a todo aluno permissão de escrita em repos compartilhados). O exercício da Final passa a ser um PR `lab-grande-final`→`main` **no próprio repositório do aluno** — nunca mais no upstream compartilhado.
   *(Fonte: EPIC-004 §Escopo Frente C — "Template Repository novo... 'Use this template' gera repo próprio do aluno (não fork) — mata a classe de bugs de merge"; plano §6 tabela "Repo".)*

2. **AC-2 — Branch `lab-grande-final` dentro do Template Repository** contém o estado curado que o "Use this template" propaga — mesmo padrão de branch curada `lab-*` já validado nas Oitavas (`lab-oitavas-de-final`) e Quartas (`lab-quartas-de-final`), agora aplicado a um repo próprio em vez de branch no upstream de um fork compartilhado. `@devops` confirma o slot exato (branch default do template vs. branch nomeada) no provisionamento.
   *(Fonte: EPIC-004 tabela de Stories linha S5 — "branch `lab-grande-final` (@devops confirma)"; precedente Oitavas/Quartas.)*

### Bloco B — `lab.config.json`: variáveis 15→1 (a maior dor)

3. **AC-3 — Baseline real a colapsar, confirmado nesta story:** a Fase 5 das Quartas (`docs/runbooks/quartas-f2-portal-guide.md`, tabela completa verificada linhas 274-301) exige do aluno **exatamente 15 Variables** (`SQL_SERVER`, `RESOURCE_GROUP`, `ACR_LOGIN_SERVER`, `PHASE04_CONTAINERAPP_NAME`, `PHASE04_RESOURCE_GROUP`, `FRONTEND_APP_NAME`, `BACKEND_APP_NAME`, `GATEWAY_V2_URL`, `BACKEND_URL`, `FUNCTION_V2_URL`, `VITE_CIAM_AUTHORITY`, `VITE_CIAM_CLIENT_ID`, `VITE_ADMIN_TENANT_ID`, `VITE_ADMIN_CLIENT_ID`, `VITE_ADMIN_SCOPE`) **+ 5 Secrets** (`AZURE_CREDENTIALS`, `SQL_CONNECTION_STRING`, `AZURE_FRONTEND_PUBLISH_PROFILE`, `AZURE_BACKEND_PUBLISH_PROFILE`, `GATEWAY_SHARED_SECRET`) — a contagem **"15 vars + 5 secrets"** do plano/épico bate exatamente com este baseline verificado (não é aproximação retórica). Esta story colapsa esse conjunto.
   *(Fonte: verificado `docs/runbooks/quartas-f2-portal-guide.md` linhas 270-301 — tabela completa lida e contada nesta story.)*

4. **AC-4 — `lab.config.json` versionado no Template Repo, com exatamente 1 campo editável pelo aluno: `sufixo`.** Formato mínimo (só valores **não-secretos**, versionável): `{"sufixo": "<iniciais-do-aluno>"}`. O workflow **resolve o arquivo in-run** (`jq -r '.sufixo' lab.config.json` → `$GITHUB_ENV`) e **deriva** todos os nomes de recurso a partir do `sufixo` seguindo a **mesma convenção de nomenclatura já estabelecida** nas Quartas (verificada: `cr<sufixo>` ACR, `cae-<sufixo>` Container Apps Environment, `ca-gateway-<sufixo>` gateway — `quartas-f2-portal-guide.md` linhas 66-68) — cobre `ACR_LOGIN_SERVER`, `PHASE04_CONTAINERAPP_NAME`/`RESOURCE_GROUP`, `FRONTEND_APP_NAME`, `BACKEND_APP_NAME`, `SQL_SERVER`, `RESOURCE_GROUP` (9 dos 15). Elimina **inteiramente** a camada de GitHub Variables (o aluno não cria nenhuma).
   *(Fonte: plano §6 — "um `lab.config.json` versionado (só não-secreto) onde o aluno edita 1 linha (sufixo); o workflow resolve o arquivo in-run (jq→$GITHUB_ENV) e deriva todos os nomes/URLs"; convenção verificada em `quartas-f2-portal-guide.md` linhas 66-68.)*

5. **AC-5 — URLs (`GATEWAY_V2_URL`, `BACKEND_URL`, `FUNCTION_V2_URL`) resolvidas por consulta dinâmica ao Azure (`az ... --query ...`), não por template de string hardcoded:** em vez de assumir o formato do FQDN (frágil — Container Apps podem ter domínio customizado, Function Apps têm slot), o workflow consulta os recursos **já existentes** (nomeados via `sufixo`, AC-4) com o `AZURE_CREDENTIALS` da turma e injeta a URL real retornada pela API do Azure em `$GITHUB_ENV`. Mais robusto que hardcoded, e ainda **zero** entrada manual do aluno.
   *(Fonte: EPIC-004 — "zero `az create`" não exclui `az ... show`/`--query` de leitura; ADE-003 "URL nunca hardcoded", princípio já estabelecido no projeto.)*

6. **AC-6 — `[AUTO-DECISION]` Vars de identidade (`VITE_CIAM_AUTHORITY`/`CLIENT_ID`, `VITE_ADMIN_TENANT_ID`/`CLIENT_ID`, `VITE_ADMIN_SCOPE`) e os 5 Secrets NÃO são re-inseridos pelo aluno neste repositório novo** — ver §"Decisão de design" abaixo (a pendência mais delicada da simplificação, resolvida nesta story).

7. **AC-7 — Único secret no GitHub do repositório do aluno: `AZURE_CREDENTIALS`** (JSON do Service Principal — "valor da turma", conforme o plano: pode ser um Service Principal **compartilhado pela turma** com acesso de leitura/deploy restrito aos recursos já nomeados por convenção, já que o T2 de rede é pré-provisionado pelo instrutor e o aluno só precisa de permissão para *ler* recursos existentes e fazer *deploy* de código sobre eles — não para criar infraestrutura nova). **Nenhum outro secret** é criado pelo aluno nesta story. Segredos reais de aplicação (LLM keys, `GATEWAY_SHARED_SECRET`) vivem **exclusivamente** no Key Vault (Story 4.1), resolvidos por **KV reference + Managed Identity** — nunca em GitHub Secret, arquivo ou log.
   *(Fonte: plano §6 — "Único secret: `AZURE_CREDENTIALS` (valor da turma). Segredos reais vivem no KV (KV references + User-Assigned MI) → nunca no GitHub/arquivo/log"; EPIC-004 SC-6.)*

### Bloco C — `lab-grande-final.yml` (Actions só código, zero `az create`)

8. **AC-8 — Workflow `workflow_dispatch` com input `acao` do tipo `choice`**, seguindo o **padrão `acao`-guardado** já validado em `lab-quartas-de-final.yml` (verificado: blocos guardados por `if: ${{ inputs.acao == 'tudo' || inputs.acao == '<bloco>' }}`, sem `needs` entre jobs — cada bloco independente e re-executável). Inclui **um novo valor: `check`** — o Doctor pré-flight (Bloco D). Os demais valores de `acao` mapeiam aos artefatos deployáveis da Final (build/deploy do McpServer, do FlowEvents, ajustes de frontend/Diploma — granularidade exata decidida por `@devops` na implementação, seguindo o mesmo princípio de blocos independentes).
   *(Fonte: verificado `.github/workflows/lab-quartas-de-final.yml` linhas 7-15, 33-40, padrão `if: ${{ inputs.acao == 'tudo' || inputs.acao == '<bloco>' }}` nos 4 blocos existentes — molde direto, reúso não invenção.)*

9. **AC-9 — Zero `az create`/`az ... provision` no workflow:** toda a topologia complexa (VNet, CAE, Private Endpoint, Key Vault, Managed Identities) foi **pré-provisionada pelo instrutor** via `browser-harness` (Bloco D/Story 4.1-4.2) antes da sessão. O workflow só faz **build + deploy de código** (`az webapp deploy`, `az containerapp update --image`, `az functionapp deployment`) e **leitura** (`az ... show`/`--query`, AC-5) sobre recursos já existentes — nunca cria recursos novos.
   *(Fonte: EPIC-004 §Escopo Frente C — "`lab-grande-final.yml`... zero `az create` (a topologia complexa foi pré-provisionada pelo instrutor)".)*

10. **AC-10 — Mantém build + unit tests** (paridade com `lab-quartas-de-final.yml`, que já roda `dotnet build`/`test` como parte do bloco `gateway`) — nenhuma regressão da qualidade de CI já estabelecida.
    *(Fonte: EPIC-004 §Escopo Frente C — "mantém build + unit tests".)*

11. **AC-11 — Imprime URLs + checklist manual ao final de cada bloco** (paridade com o padrão já usado — verificado `lab-quartas-de-final.yml` linha ~350, "Checkpoint" com URLs e status esperado).
    *(Fonte: EPIC-004 §Escopo Frente C — "imprime URLs+checklist manual"; padrão herdado.)*

### Bloco D — `acao=check` (Doctor pré-flight) + runbook `browser-harness`

12. **AC-12 — `acao=check` executa o Doctor pré-flight, consumindo diretamente o checklist da Story 4.4 (AC-11):** valida, sem alterar nada (`az ... show`/`--query`, read-only), os 5 itens conhecidos de falha de deploy (RBAC do SP no RG do gateway; ACR conectado nos Registries do Container App; `ingress.targetPort = 8080`; health probes na mesma porta; nome exato do segredo `Gateway__AdminSharedSecret`/`GATEWAY_SHARED_SECRET` — consolidados em `docs/runbooks/deploy-preflight-checklist.md`, Story 4.4) **e** confirma que o Key Vault/Managed Identity (Story 4.1) estão acessíveis e resolvendo (smoke de leitura de um secret via a MI, sem expor o valor em log). Resultado: um resumo PASS/FAIL por item, impresso no job summary — o aluno roda `acao=check` **antes** de `acao=tudo` e só prossegue com tudo PASS.
    *(Fonte: EPIC-004 §Escopo Frente C — "`acao=check` (Doctor pré-flight)"; Story 4.4 AC-11/AC-12 — handoff explícito do checklist como insumo direto.)*

13. **AC-13 — Runbook `browser-harness` (instrutor, offline) documenta o pré-provisionamento único da topologia T2 + a semeadura de segredos no Key Vault** — a skill `browser-harness` (já instalada no ambiente do squad) é a ferramenta certa porque **para em auth walls** (login interativo do Portal Azure) e por isso **não escala em CI** — é deliberadamente ferramenta de instrutor, executada **uma vez**, fora do relógio da aula (plano §9.4 "proteger o clímax"). O runbook cobre: criação da VNet + CAE VNet-integrado (**ordem dura** — CAE é imutável, nasce antes de qualquer app, ADE-009 Inv 3), Private Endpoint do SQL, Key Vault + Managed Identities semeadas, e a sequência correta antes de `VITE_*`/CORS serem fixados no frontend (recriar a VNet muda o FQDN do gateway — aprendizado registrado das Quartas).
    *(Fonte: EPIC-004 §Escopo Frente C — "browser-harness = instrutor, offline — runbook de pré-provisionamento... para em auth walls → não escala em CI"; ADE-009 Inv 3; aprendizado das Quartas sobre FQDN.)*

### Bloco E — UX do aluno e anti-hallucination

14. **AC-14 — Dry-run do fluxo do aluno validado (sem provisionar nada de verdade):** "Use this template" → editar `sufixo` → configurar `AZURE_CREDENTIALS` → `acao=check` (PASS) → `acao=tudo` — nenhum passo manual de GitHub Variables, nenhum segredo de aplicação digitado pelo aluno. Esta é a validação central de `@po` (UX do aluno).
    *(Fonte: EPIC-004 SC-6 — "Entrega sem-atrito... dry-run do fluxo do aluno (plano §6/§10)".)*

15. **AC-15 — Regressão: Oitavas/Quartas não são afetadas.** Esta story cria um repositório **novo** e um workflow **novo** — não modifica `lab-oitavas-de-final.yml`/`lab-quartas-de-final.yml` nem os forks existentes dos alunos.
    *(Fonte: EPIC-004 Compatibility Requirements.)*

16. **AC-16 — Anti-hallucination (Art. IV):** "GitHub Template Repository" é uma funcionalidade nativa e real do GitHub (Settings → Template repository), distinta de "fork" — não inventada. `jq` é a ferramenta real já usada no padrão `acao`-guardado (nenhuma nova dependência de parsing JSON introduzida). Os 15 nomes de variável e 5 nomes de secret citados nesta story foram lidos diretamente da tabela real do runbook das Quartas, não de memória aproximada.

---

## `[AUTO-DECISION]` — a pendência mais delicada da simplificação, resolvida aqui

### Vars de identidade (CIAM/Admin) e Secrets de deploy num repositório NOVO → **consultados dinamicamente, não re-inseridos**

**O problema:** `VITE_CIAM_AUTHORITY`/`CLIENT_ID`, `VITE_ADMIN_TENANT_ID`/`CLIENT_ID` são variáveis de **build-time do frontend** (Vite as embute no bundle estático — não há "runtime" de KV reference para uma SPA já compilada). `SQL_CONNECTION_STRING`, `AZURE_FRONTEND_PUBLISH_PROFILE`, `AZURE_BACKEND_PUBLISH_PROFILE` são específicas do aluno. Como um repositório **novo** (Template Repo, não uma continuação do fork das Quartas) resolve esses valores sem pedir ao aluno para digitá-los de novo?

**Decisão:** o workflow **consulta dinamicamente** os recursos Azure **já existentes** do aluno (criados nas Oitavas/Quartas, nomeados pela **mesma convenção** de `sufixo` — AC-4) usando o `AZURE_CREDENTIALS` da turma:
- `SQL_CONNECTION_STRING` **deixa de existir** — a Story 4.1 já move as conexões de aplicação para MI-AAD; não há connection string com senha para propagar.
- `AZURE_FRONTEND_PUBLISH_PROFILE`/`AZURE_BACKEND_PUBLISH_PROFILE` são substituídos por `az webapp deploy`/`az functionapp deployment` autenticados via `AZURE_CREDENTIALS` (Service Principal) — **nenhum publish profile** precisa ser baixado/colado pelo aluno.
- `VITE_CIAM_*`/`VITE_ADMIN_*` (build-time do frontend): o workflow **lê os App Settings já configurados** no frontend App Service existente do aluno (`az webapp config appsettings list`, ou lê os equivalentes já persistidos nas GitHub Variables/Secrets do FORK original das Quartas, se o `@devops` decidir reusar aquele repositório como fonte — decisão de implementação) e os injeta como `VITE_*` no build **sem** o aluno redigitar. Se o Diploma/chatbot não exigirem rebuild do frontend inteiro (só rotas novas adicionadas), este passo pode nem ser necessário — **decisão final de arquitetura de build cabe a `@dev`/`@devops` na implementação**, registrada aqui como direção, não como literal.

**Por quê (justificativa):**
- **Honra a "URL/valor nunca hardcoded" (ADE-003) e "zero segredo no GitHub" (ADE-009 Inv 2) simultaneamente** — em vez de inventar um mecanismo novo de propagação de segredo, reusa a MESMA fonte da verdade (os recursos Azure reais do próprio aluno) que já existe.
- **Evita reintroduzir GitHub Variables pela porta dos fundos** — se o aluno tivesse que re-digitar os 5-6 valores de identidade no repositório novo, a simplificação "15→1" seria só nominal.
- **Alternativa registrada (não escolhida):** pedir ao aluno para copiar manualmente os valores do fork antigo para o repo novo — rejeitada por reintroduzir exatamente a fricção que esta story existe para eliminar.

---

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** Deployment (Template Repository + CI/CD + workflow de entrega)
**Secondary Type(s):** Security (zero secret no GitHub além de `AZURE_CREDENTIALS`), Documentation (runbook `browser-harness`)
**Complexity:** HIGH (múltiplas peças móveis — repositório novo, schema de config, workflow, resolução dinâmica de vars de identidade — e depende de infraestrutura de 2 outras stories, 4.1 e 4.4)

### Specialized Agent Assignment

**Primary Agents:**
- @devops (Gage) — Template Repository, workflow, CI/CD (autoridade exclusiva)
- @dev (Dex) — schema/resolução de `lab.config.json`

**Supporting Agents:**
- @analyst (Atlas) — runbook `browser-harness` narrativo
- @architect (Aria) — quality gate (Inv 2/3)
- @po (Pax) — validação de UX do aluno

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): `coderabbit --prompt-only -t uncommitted` — foco em nenhum secret de aplicação em `lab.config.json` ou em log de workflow
- [ ] Pre-PR (@devops): `coderabbit --prompt-only --base main` — confirmar zero `az create` no workflow
- [ ] Pre-Deployment (@devops): dry-run completo do fluxo do aluno (AC-14) antes de publicar o Template Repository para a turma

### Self-Healing Configuration

**Expected Self-Healing:**
- Primary Agent: @devops (check mode)
- Max Iterations: 0
- Timeout: N/A
- Severity Filter: report_only

**Predicted Behavior:**
- Todas as severidades: report_only (workflow/infra — `@devops` decide manualmente, sem auto-fix)

### CodeRabbit Focus Areas

**Primary Focus:**
- `lab.config.json` contém **só** `sufixo` — nenhum campo secreto adicionado por engano
- Workflow não tem nenhum `az ... create`/`az ... provision` escondido num bloco

**Secondary Focus:**
- `acao=check` é genuinamente read-only (nenhum `az ... set`/`update` no Doctor)
- Mensagens de erro do Doctor não vazam valores de secret (só PASS/FAIL por item)

---

## Tasks / Subtasks

### Task 1 — Template Repository (AC: 1, 2)

- [x] 1.1 (@devops) Criar repositório `fifa2026-final-lab` na org TFTEC; habilitar "Template repository" nas Settings. **FEITO 2026-07-02 (owner autorizou "pode criar como privado"):** repo **privado** `https://github.com/TFTEC/fifa2026-final-lab` criado com `is_template=true`. Semeado de `copa-azure-final@67a5b00` (código da Final completo) + na raiz: `lab.config.json`, `.github/workflows/lab-grande-final.yml` (ÚNICO workflow — estado curado, precedente das Quartas) e `README.md` do aluno (bloco "Conteúdo do README-raiz" do staging). Commit único `5a1ee80`, 647 arquivos. **Pendente do owner: tornar público/interno quando a turma for usar.**
- [x] 1.2 (@devops) Criar/confirmar branch `lab-grande-final` como o estado curado propagado pelo "Use this template". **FEITO:** `lab-grande-final` é a **default branch** do repo (é ela que o "Use this template" propaga).

### Task 2 — `lab.config.json` (AC: 3, 4, 5, 6, 7)

- [x] 2.1 Baseline reconfirmado: 15 vars + 5 secrets de `quartas-f2-portal-guide.md` (tabela de nomes L54-76; convenção `cr`/`cae`/`ca-gateway` L66-68).
- [x] 2.2 Schema `lab.config.json` = `{"sufixo": "..."}` (1 campo, só não-secreto) criado; convenção derivada `<prefixo>-<sufixo>` documentada no `README.md` de staging (contrato com o pré-provisionamento do instrutor).
- [x] 2.3 Resolução in-run (`jq -r '.sufixo' lab.config.json` → `$GITHUB_ENV`) implementada no step "Resolver lab.config.json" (valida placeholder/vazio + charset do ACR).
- [x] 2.4 URLs por consulta dinâmica (`az containerapp/webapp/functionapp show --query`) implementadas no bloco `frontend` (`GATEWAY_V2_URL`/`BACKEND_URL`/`FUNCTION_V2_URL`) — nunca hardcoded.
- [x] 2.5 Vars de identidade `VITE_*` resolvidas via `az webapp config appsettings list` do frontend existente (fonte semeada pelo instrutor), com fail-fast se ausente — decisão registrada no Dev Agent Record.
- [x] 2.6 Único GitHub Secret = `AZURE_CREDENTIALS` (nenhum `secrets.*` além dele no workflow).

### Task 3 — `lab-grande-final.yml` (AC: 8, 9, 10, 11)

- [x] 3.1 Workflow criado com input `acao` (`choice`): `check | tudo | mcp-server | flow-events | gateway | frontend`; padrão `acao`-guardado sem `needs` (herdado de `lab-a-final.yml`).
- [x] 3.2 Zero `az create`/`provision` confirmado por parse (só `update --image`/`webapp deploy`/`show`/`list`/`--query`).
- [x] 3.3 `dotnet build`/`test` mantidos nos 3 blocos .NET (mcp-server, gateway, flow-events).
- [x] 3.4 URLs + checklist impressos no `$GITHUB_STEP_SUMMARY` ao fim de cada bloco.

### Task 4 — `acao=check` Doctor (AC: 12)

- [x] 4.1 5 itens read-only do checklist da 4.4 implementados (RBAC, ACR, ingress 8080, probes 8080, nome do segredo) + a invariante de perímetro do @architect (item 6).
- [x] 4.2 Smoke KV/MI (item 7): confirma secret via `keyVaultUrl` + revisão ativa `Running` (a MI resolveu o KV ref) — só metadados, nunca o valor.
- [x] 4.3 Resultado PASS/FAIL por item no job summary; `exit 1` se qualquer FAIL. Doctor sem `set -e` (coleta todos os itens); nenhum `az set/update/create`.

### Task 5 — Runbook `browser-harness` (AC: 13)

- [x] 5.1 (@analyst) Documentar o passo-a-passo de pré-provisionamento (VNet+CAE, PE do SQL, Key Vault + MIs) na ordem correta (ADE-009 Inv 3). **→ `docs/runbooks/browser-harness-final-preprovisioning.md` criado — runbook narrativo de instrutor: Bloco 1 (VNet + CAE VNet-integrado PRIMEIRO, imutável), Bloco 2 (Private Endpoint do SQL + DNS privado), Bloco 3 (Key Vault + UA-MI `id-fifa2026-kv-reader` + system-assigned por-app), Bloco 4 (FQDN estável ANTES de `VITE_*`/CORS). Cada bloco com "porquê" + "o que quebra se inverter" (aprendizado das Quartas sobre o FQDN) + portão de validação.**
- [x] 5.2 (@analyst) Documentar a semeadura de segredos no Key Vault (execução única, offline). **→ Bloco 3.4 do runbook: semeadura byte-a-byte de `gateway-admin-shared-secret`/`gemini-api-key`/`sql-connection-string`/`azure-signalr-connection-string`, offline via `az keyvault secret set`, ninguém referencia ainda (risco zero); ganho estrutural do secret único do X-Gateway-Key registrado (ADE-010 D3). Enquadrado como "proteger o clímax" (plano §9.4) + handoff para `acao=check` (Doctor smoke de leitura via MI).**

### Task 6 — UX e regressão (AC: 14, 15, 16)

- [ ] 6.1 (@po) Dry-run completo do fluxo do aluno (sem provisionar recursos reais) — validar "1 campo + 1 secret". **PENDENTE — gate @po.**
- [x] 6.2 `lab-oitavas-de-final.yml`/`lab-quartas-de-final.yml`/`lab-a-final.yml` intocados (entrega vive em `docs/entrega/`, staging isolado — `.github/workflows/` do staging não roda do root; nenhum workflow existente modificado).
- [x] 6.3 Anti-hallucination: "GitHub Template Repository" é feature nativa real; `jq` já em uso; os 15 nomes de var + 5 secrets lidos da tabela real das Quartas; convenção `<prefixo>-<sufixo>` estende a já validada (não inventada) e é declarada como contrato explícito com o runbook do instrutor.

---

## Dev Notes

### O que colapsa e como (mapa 15→1)

| Categoria (Quartas) | Quantidade | Destino nesta story |
|---|---|---|
| Nomes de recurso derivados de `sufixo` (`SQL_SERVER`, `RESOURCE_GROUP`, `ACR_LOGIN_SERVER`, `PHASE04_CONTAINERAPP_NAME`, `PHASE04_RESOURCE_GROUP`, `FRONTEND_APP_NAME`, `BACKEND_APP_NAME`) | 7 vars | Derivados do `sufixo` (1 campo) + convenção fixa |
| URLs (`GATEWAY_V2_URL`, `BACKEND_URL`, `FUNCTION_V2_URL`) | 3 vars | Consultadas dinamicamente via `az ... --query` |
| Identidade frontend (`VITE_CIAM_AUTHORITY`/`CLIENT_ID`, `VITE_ADMIN_TENANT_ID`/`CLIENT_ID`, `VITE_ADMIN_SCOPE`) | 5 vars | Consultadas dinamicamente dos recursos já configurados (`[AUTO-DECISION]`) |
| `SQL_CONNECTION_STRING` | 1 secret | **Eliminado** — MI-AAD (Story 4.1) |
| `AZURE_FRONTEND_PUBLISH_PROFILE`/`AZURE_BACKEND_PUBLISH_PROFILE` | 2 secrets | **Eliminados** — deploy via `AZURE_CREDENTIALS` (Service Principal) |
| `GATEWAY_SHARED_SECRET` | 1 secret | **Eliminado do GitHub** — vive no Key Vault (Story 4.1), lido por KV reference + MI |
| `AZURE_CREDENTIALS` | 1 secret | **Permanece** — o único secret desta story |
| **TOTAL** | **15 vars + 5 secrets** | **1 campo (`sufixo`) + 1 secret (`AZURE_CREDENTIALS`)** |

### Arquitetura técnica delta

```
[fifa2026-final-lab (repo NOVO, Template)]
  lab.config.json                → { "sufixo": "<iniciais>" }
  .github/workflows/lab-grande-final.yml
    acao=check   → Doctor pré-flight (5-fixes checklist da 4.4 + smoke KV/MI)
    acao=<bloco> → deploy de código sobre infra já pré-provisionada (zero az create)
    acao=tudo    → todos os blocos

[Resolução in-run]
  jq -r '.sufixo' lab.config.json → $GITHUB_ENV
  nomes derivados: cr<sufixo>, cae-<sufixo>, ca-gateway-<sufixo>, ...
  URLs: az ... --query (dinâmico, não hardcoded)
  identidade frontend: consulta aos recursos já configurados do aluno
```

### Arquivos tocados (File List esperada)

| Arquivo/Recurso | Ação |
|---|---|
| Repositório `fifa2026-final-lab` (GitHub, org TFTEC) | **Novo** |
| `lab.config.json` (no repo novo) | **Novo** |
| `.github/workflows/lab-grande-final.yml` (no repo novo) | **Novo** |
| `docs/runbooks/browser-harness-final-preprovisioning.md` (nome sugerido, neste repo ou no novo) | **Novo** |

**NÃO tocam:** `lab-oitavas-de-final.yml`, `lab-quartas-de-final.yml` (neste repositório) — permanecem intocados; os forks existentes dos alunos das Oitavas/Quartas não são afetados.

### Verificação na fonte da verdade (Art. IV)

| Afirmação | Confirmado em |
|---|---|
| Tabela completa de 15 vars + 5 secrets das Quartas | `docs/runbooks/quartas-f2-portal-guide.md` linhas 270-301 |
| Convenção `cr<sufixo>`/`cae-<sufixo>`/`ca-gateway-<sufixo>` | `quartas-f2-portal-guide.md` linhas 54-68 |
| Padrão `acao`-guardado (`if: inputs.acao == 'tudo' \|\| inputs.acao == '<bloco>'`) | `.github/workflows/lab-quartas-de-final.yml` linhas 7-15, 33-348 |
| `dotnet build/test` já parte do bloco `gateway` | `lab-quartas-de-final.yml` linha 333 |

### Testing

**Test file locations:** N/A (story de infraestrutura/workflow — não há testes .NET/JS novos; a validação é o dry-run, AC-14, e o Doctor `acao=check` em si)

**Test standards:**
- Dry-run manual/observado do fluxo completo do aluno (Task 6.1) é o critério de aceite prático.
- `acao=check` deve ser idempotente e seguro de rodar repetidamente (read-only).

---

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-07-01 | 0.1 | Story drafted — Entrega/Capstone (Frente C do EPIC-004). 16 ACs derivados do plano §6 + EPIC-004 §Escopo Frente C. Baseline "15 vars + 5 secrets" verificado linha-a-linha em `quartas-f2-portal-guide.md` (não aproximação) — mapa completo de destino de cada variável documentado em Dev Notes. Resolvida a pendência de design mais delicada (`[AUTO-DECISION]`): vars de identidade do frontend num repositório NOVO são consultadas dinamicamente dos recursos Azure já existentes do aluno, não re-inseridas — evita reintroduzir GitHub Variables pela porta dos fundos. Molde: Story 2.11 (formato) + `lab-quartas-de-final.yml` (padrão `acao`-guardado, reusado literalmente). | @sm (River) |
| 2026-07-01 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready. **@po (autoridade de backlog) CONFIRMA Capstone in-épico (4.5/4.6), NÃO EPIC-005** — as 5 razões do épico procedem (sem gate próprio ⇒ não é fronteira de épico; amarração dura à ADE-009 Inv 2/3; precedente 2.7/3.6 transversal in-épico; dimensionamento contido pelo pré-provisionamento). Baseline 15→1 verificado. Leve dedução: AC-6 deixa a resolução das vars `VITE_*` build-time como decisão de implementação (@dev/@devops) — honestamente sinalizada com direção clara, não gap. Delegações Template Repo/CI→@devops declaradas. | @po (Pax) |
| 2026-07-02 | 0.2 | **Code review remediation (@dev Dex)** — 4 findings do review "APROVADO COM RESSALVAS" fechados: F1 (MEDIUM) reconciliação da nomenclatura runbook↔workflow via opção (b) (seção "## Nomenclatura" + mapeamento HML→convenção, menos invenção — Art. IV); F2 (LOW) fail-fast das VITE_* simétrico (CIAM + admin workforce); F3 (LOW) deploy zip documentado como intencional (`az webapp deploy` AAD-auth, publish profiles eliminados no 15→1); F4 (LOW) `jq` com `\|\| true` para a mensagem amigável do guard do `sufixo` sempre sair sob `set -e`. Retro-compat/invariantes verificadas (zero `az create`, único secret `AZURE_CREDENTIALS`); YAML revalidado (js-yaml, 20 steps). Status inalterado (gates @architect/@po pendentes). | @dev (Dex) |
| 2026-07-02 | 0.2.1 | **Re-review remediation (@dev Dex)** — O1 (MEDIUM, pré-existente): contradição doc↔doc — o fail-fast do workflow e o AC-6 diziam que o instrutor semeia as `VITE_*` de identidade no pré-provisionamento, mas o runbook não tinha esse passo e listava `VITE_*` como "fica para a aula". Fechado pelo caminho de menor invenção: as 5 `VITE_*` de identidade **não** dependem do FQDN e o workflow as **lê** dos App Settings do frontend → o **runbook ganhou o passo de semeá-las** (Bloco 4, `az webapp config appsettings set` no `app-frontend-<sufixo>`, valores não-secretos OIDC) + a linha "fica para a aula" reescopada só para `VITE_*` de URL/CORS (FQDN-dependentes) + linhas na tabela "vai deixar pronto" e no checkpoint. Workflow, story e runbook agora contam a MESMA história; nenhum recurso novo inventado. O4 (cosmético): Change Log reordenado (v0.2 estava fora de sequência). | @dev (Dex) |
| 2026-07-02 | 0.2.2 | **Gate remediation (@dev Dex)** — @architect PASS (4 dimensões); @po GO condicional 8/10 com 2 MEDIUM + 2 LOW, fechadas: **MEDIUM #1** Doctor ganhou **item 8** (presença read-only das 5 `VITE_*` de identidade no frontend, nunca valores) + contagem "7→8 checks" propagada (workflow/README/Completion Notes); **MEDIUM #2** wiring das KV-references dos Container Apps declarado como pré-provisionamento no runbook (Bloco 3 passo 5, referenciando o `final-portal-guide.md` sem reabri-lo) + "fica para a aula" reescopado só p/ FQDN — runbook ↔ Doctor item 7 coerentes; **LOW #4** proveniência do `AZURE_CREDENTIALS` (é da turma) no README; **LOW #5** README-raiz do aluno (15→1 + "o que mudou desde as Quartas") no plano de publicação. Parecer do @po colado na §QA Results (abaixo do @architect, cuja auditoria de "7 checks" foi preservada intacta — pré-item-8). YAML revalidado (js-yaml). Status inalterado. | @dev (Dex) |
| 2026-07-02 | 0.2.3 | **Cosmetic polish (@dev Dex)** — 3 LOWs do re-review final: (1) linha dos Container Apps wired às KV references na tabela "O que você vai deixar pronto" do runbook (coerência com Bloco 3 passo 5 + checkpoint); (2) nota `[confirmar]` sobre a imagem seed no Bloco 3 passo 5 (criar Container App exige imagem inicial + revisão `Running` antes do `update --image`; usar a imagem do `final-portal-guide` ou placeholder funcional — ponto a confirmar com o owner, sem inventar nome de imagem); (3) Change Log reordenado para 0.2 → 0.2.1 → 0.2.2. Sem mudança funcional. | @dev (Dex) |

---

## Dev Agent Record

### Agent Model Used

@devops (Gage) — claude-opus-4-8. Execução em 2026-07-02 no worktree `lab-a-final`. (Task 5 pelo @analyst.)

### Completion Notes

**Escopo desta execução (@devops):** núcleo da entrega (Tasks 2, 3, 4 + 6.2/6.3) produzido como **conteúdo de staging** em `docs/entrega/fifa2026-final-lab/` — a criação do repo real (Task 1) e o dry-run @po (6.1) ficam pendentes. Task 5 (runbook browser-harness) entregue pelo @analyst em paralelo.

**Decisão de build da identidade `VITE_*` (AC-6 / [AUTO-DECISION], Task 2.5) — registro formal:**
- O bloco `frontend` **rebuilda** o bundle (chatbot F5, rota `/flow` F6 e Diploma são embutidos por Vite → exigem as `VITE_*` em build-time; não há runtime de KV reference para SPA compilada).
- As 5 vars de identidade são **lidas dinamicamente** dos **App Settings do frontend Web App existente** (`az webapp config appsettings list`) — o instrutor as semeia no pré-provisionamento (runbook do @analyst) como fonte-da-verdade. **Fail-fast** com `::error` se ausentes (aponta ao runbook), evitando bundle com login morto.
- **Alternativa não escolhida:** pedir ao aluno re-digitar as 5 vars → rejeitada (reintroduziria GitHub Variables pela porta dos fundos, anulando o 15→1).
- **Evolução possível (não aplicada):** se o Diploma/chatbot forem entregues sem exigir rebuild, o bloco `frontend` pode virar só-deploy. A Final rebuilda, por paridade com `lab-a-final.yml`.

**Segredos de aplicação:** os blocos de deploy **não** aplicam nenhum secret (`az containerapp secret set` ausente) — SQL/Gemini/X-Gateway-Key vêm do **Key Vault via KV reference + Managed Identity** já pré-wired pelo instrutor. Honra AC-7 (só `AZURE_CREDENTIALS`) e ADE-009 Inv 2 (zero segredo no GitHub/log).

**Doctor `acao=check`:** 8 itens read-only (5 gotchas da 4.4 + invariante de perímetro do @architect + smoke KV/MI + presença das 5 `VITE_*` de identidade — item 8, adicionado no gate @po MEDIUM #1). O smoke KV/MI valida a resolução da MI consultando **só metadados** (secret com `keyVaultUrl` + revisão `Running`) — nunca o valor. O item 8 confere só a **presença do nome** das 5 `VITE_*` no App Settings do frontend — nunca valores. Zero `az set/update/create`.

**Convenção de nomes (contrato):** o `sufixo` deriva `<prefixo>-<sufixo>` para todos os recursos (estende `cr`/`cae`/`ca-gateway` das Quartas). É um **contrato explícito** com o runbook do @analyst, documentado no `README.md` de staging. Divergência honesta vs. Quartas: lá frontend/func/sql/backend eram nomes livres; aqui a entrega exige que o instrutor os provisione por convenção — é o que torna o "1 campo" possível.

### Code review remediation (2026-07-02, @dev Dex) — 4 findings do review "APROVADO COM RESSALVAS"

Todas as 4 ressalvas fechadas. Nenhum AC alterado; retro-compat preservada (Oitavas/Quartas/`lab-a-final.yml` intocados); invariantes mantidas (verificado por parse: zero `az create`/`provision` real — só comentários; único `secrets.*` = `AZURE_CREDENTIALS`). YAML revalidado com `js-yaml` (20 steps, `acao` = check/tudo/mcp-server/flow-events/gateway/frontend inalterado).

- **Finding 1 (MEDIUM — reconciliação da nomenclatura):** o runbook `browser-harness-final-preprovisioning.md` citava nomes HML legado (`kv-dev-tk-cin-001`, `id-fifa2026-kv-reader`) enquanto o workflow/README derivam `<prefixo>-<sufixo>`. **Opção (b)** escolhida (menos invenção, Art. IV): declarar explicitamente que os nomes concretos do runbook são o **ambiente HML de validação do instrutor** e fornecer a **tabela de mapeamento HML → convenção `<prefixo>-<sufixo>`**. Adicionada a seção **"## Nomenclatura"** (mapeamento dos 11 recursos derivados + a exceção da UA-MI, que o workflow não referencia por nome) + 3 anotações cruzadas (tabela de recursos, Bloco 3.1, checklist). A convenção do AC-4 (validada nas Quartas — `cr`/`cae`/`ca-gateway`) permanece o contrato oficial; runbook e workflow agora contam a MESMA história. Nenhum nome inventado (todos já existem no workflow `$GITHUB_ENV`/README ou são o RG/KV reais do HML). `final-portal-guide.md` **não** foi tocado.
- **Finding 2 (LOW — fail-fast assimétrico):** o pré-build do frontend validava só `VITE_CIAM_*`. Tornado **simétrico**: agora valida as 5 VITE_* obrigatórias (CIAM **+** admin workforce: `VITE_ADMIN_TENANT_ID`/`CLIENT_ID`/`SCOPE`), listando todas as ausentes numa única mensagem. Implementado com blocos `if` (nunca `&&`) para não disparar o `set -e` quando a variável está presente.
- **Finding 3 (LOW — deploy por zip):** confirmado que `az webapp deploy --type zip` é **intencional e correto** — não é desvio: as Quartas usam `azure/webapps-deploy@v3` + `publish-profile`, mas a Final **elimina** os publish profiles (colapso 15→1, AC-7). `az webapp deploy` autentica pela sessão `az` (token AAD do SP), então **não** depende de SCM basic auth nem de publish profile, e é o sucessor recomendado do `config-zip` (deprecado); o bundle é buildado no step anterior. **Documentado inline** no step de deploy.
- **Finding 4 (LOW — guard sob `set -e`):** o guard de presença do campo `sufixo` fazia `SUFIXO=$(jq -r ... lab.config.json)` sob `set -euo pipefail` — um `lab.config.json` com JSON malformado mataria o step (jq exit != 0) **antes** da mensagem amigável "campo 'sufixo' não preenchido". Adicionado `2>/dev/null || true` ao `jq` → SUFIXO fica vazio e o guard `if [ -z "$SUFIXO" ]` **sempre** emite o erro amigável.

**Re-review adversarial (2026-07-02):** os 4 fixes acima APROVADOS (zero problema novo). 1 observação MEDIUM pré-existente fechada:

- **O1 (MEDIUM, pré-existente — contradição doc↔doc):** o fail-fast do frontend (workflow) e o AC-6 diziam que o instrutor semeia as `VITE_*` de identidade no pré-provisionamento (runbook), mas o runbook **não** tinha esse passo e listava `VITE_*` como "fica para a aula". Como as 5 `VITE_*` de identidade **não** dependem do FQDN do gateway e o workflow as **lê** dos App Settings do frontend (AC-6), o caminho de menor invenção foi **dar ao runbook o passo de semeá-las** (Bloco 4: `az webapp config appsettings set` no `app-frontend-<sufixo>` — valores OIDC não-secretos: tenant/client IDs + scope) e **reescopar** a linha "fica para a aula" apenas para as `VITE_*` de URL/CORS (FQDN-dependentes, resolvidas in-run pelo workflow). Adicionadas também a linha "Identidade do frontend" na tabela "vai deixar pronto" e um item no checkpoint. **Workflow, story e runbook agora contam a MESMA história**; nenhum recurso novo inventado; zero segredo (IDs/scope são identificadores públicos do OIDC, já embutidos no bundle por desenho). Sem mudança no workflow (a mensagem do fail-fast já apontava para o runbook, que agora honra o passo).

### File List

_Baseline esperado na tabela "Arquivos tocados" (Dev Notes). Entregue até aqui:_

| Arquivo | Ação | Task | Autor |
|---|---|---|---|
| `docs/runbooks/browser-harness-final-preprovisioning.md` | **Novo** — runbook de instrutor (pré-provisionamento T2 + semeadura de segredos, browser-harness offline). **Modificado no review (Finding 1 + O1)**: seção "## Nomenclatura" (HML de validação vs. convenção `<prefixo>-<sufixo>`) + 3 anotações cruzadas (F1); passo de semeadura das 5 `VITE_*` de identidade no App Settings do frontend + reescopo do "fica para a aula" + linhas na tabela/checkpoint (O1). **Gate @po MEDIUM #2**: Bloco 3 passo 5 (Container Apps nascem wired às KV references — pré-req do Doctor item 7, referencia `final-portal-guide.md`) + "fica para a aula" só p/ FQDN + handoff/checkpoint atualizados (8 checks). | 5.1 / 5.2 · review F1/O1 · gate M2 | @analyst (Atlas) · @dev (Dex) |
| `docs/entrega/fifa2026-final-lab/README.md` | **Novo** — nota de staging + mapa 15→1 + convenção + tabela do Doctor + pendências. **Gate @po**: Doctor tabela +item 8 (MEDIUM #1); proveniência do `AZURE_CREDENTIALS` no fluxo do aluno (LOW #4); seção "Conteúdo do README-raiz (aluno)" p/ Task 1 (LOW #5). | 2.2 · gate M1/L4/L5 | @devops (Gage) · @dev (Dex) |
| `docs/entrega/fifa2026-final-lab/lab.config.json` | **Novo** — `{"sufixo": "suasiniciais"}` (1 campo editável) | 2.2 | @devops (Gage) |
| `docs/entrega/fifa2026-final-lab/.github/workflows/lab-grande-final.yml` | **Novo** — workflow único (`acao` = check/tudo/mcp-server/flow-events/gateway/frontend); staging (não roda do root). **Modificado no review (Findings 2/3/4)**: fail-fast simétrico das VITE_* (F2); comentário do deploy zip intencional (F3); `jq` com `\|\| true` no guard do `sufixo` sob `set -e` (F4). **Gate @po MEDIUM #1**: Doctor `acao=check` ganhou o **item 8** (presença read-only das 5 `VITE_*` de identidade no frontend) + contagem "8 checks". | 2.3–2.6, 3.x, 4.x · review F2/F3/F4 · gate M1 | @devops (Gage) · @dev (Dex) |

**NÃO tocados (AC-15/6.2):** `lab-a-final.yml`, `lab-quartas-de-final.yml`, `lab-oitavas-de-final.yml`, `deploy-phase-*.yml`.

_Pendente: repositório real `fifa2026-final-lab` na org TFTEC (Task 1 — autorização do owner); dry-run @po (6.1)._

---

## QA Results

### Gate `@architect` (Aria) — 2026-07-02 — **PASS** ✅

**Escopo auditado (delta uncommitted sobre `c19eec1`):** `lab-grande-final.yml`, `browser-harness-final-preprovisioning.md`, `lab.config.json` (committado), `README.md` (committado). Verificação por leitura integral + parse dirigido (grep de verbos `az`, `secrets.*`, uso de env). Guia `final-portal-guide.md` **não reaberto** (gate @qa PASS anterior respeitado).

#### 1. infra-review — **PASS**
- **Convenção `<prefixo>-<sufixo>` = contrato explícito e coerente.** O workflow deriva 11 recursos do único campo `sufixo` em `$GITHUB_ENV` (L94-108: `rg-`/`cr`/`cae-`/`ca-gateway-`/`ca-mcp-`/`ca-flow-`/`app-frontend-`/`app-backend-`/`func-f1-`/`sql-`/`kv-`) — a MESMA lista que a tabela de mapeamento HML→convenção do runbook (§Nomenclatura) e a tabela do README. A UA-MI é a única exceção declarada (o workflow nunca a referencia por nome; validada indiretamente via `keyVaultUrl` do McpServer) — decisão correta e bem-justificada. Estende `cr`/`cae`/`ca-gateway` das Quartas (reúso, não invenção — Art. IV).
- **Zero `az create`/`provision` — confirmado por parse.** Os únicos verbos `az` mutantes são `containerapp update --image` (deploy de imagem), `webapp deploy --type zip` e `acr login`. Todo o resto é `show`/`list`/`--query`/`account show`/`role assignment list`/`revision list` (leitura). Os matches de `create`/`provision`/`set` no grep caíram **só em comentários** (L23, L120, L217). Honra AC-9.
- **Doctor `acao=check` read-only — 7 checks.** L123-200: `set -uo pipefail` (sem `-e`, coleta todos os itens antes do `exit 1`), nenhum `az set/update/create`. Itens 1-5 = os 5 gotchas da 4.4 (RBAC, ACR, ingress 8080, probes 8080, nome do segredo X-Gateway-Key); item 6 = invariante de perímetro; item 7 = smoke KV/MI (secret com `keyVaultUrl` + revisão `Running`). NÃO faz parte de `tudo` (pré-flight separado, default do `choice`) — correto.
- **Blocos `acao`-guardados, independentes, sem `needs`** (`if: inputs.acao == 'tudo' || inputs.acao == '<bloco>'`), re-executáveis — molde de `lab-quartas-de-final.yml`. Ordem `tudo`: mcp-server → gateway → flow-events → frontend, coerente (frontend por último, resolve URLs dos apps já atualizados). Setup .NET/ACR-login guardados nos 4 blocos .NET. `dotnet build`/`test` mantidos nos 3 blocos .NET (AC-10). URLs+checklist no `$GITHUB_STEP_SUMMARY` ao fim de cada bloco (AC-11).
- **Coerência com ADE-010:** secrets de app (SQL/Gemini/X-Gateway-Key/SignalR) só no KV via KV reference + MI; o smoke do Doctor valida a resolução da MI sem tocar o valor. Runbook cita D1/D2/D3 explicitamente e semeia byte-a-byte offline. Alinhado.

#### 2. security-validation — **PASS**
- **Único GitHub Secret = `AZURE_CREDENTIALS`** (L114). Confirmado por grep: o outro match de `secrets.` (L421) é comentário citando o padrão antigo das Quartas. Honra AC-7 + ADE-009 Inv 2.
- **Zero segredo de app em log/versionado.** Nenhum `az containerapp secret set` / `az webapp config appsettings set` de valor secreto no workflow. Segredos de app vivem no KV; o deploy só sobe imagem/bundle.
- **Doctor não vaza secret.** Item 5 valida **presença do nome** (`Gateway__AdminSharedSecret`/`GATEWAY_SHARED_SECRET`), item 7 valida **metadados** (`secrets[?keyVaultUrl].name` + `runningState`) — nunca o valor. Correto.
- **As 5 `VITE_*` de identidade semeadas são identificadores OIDC públicos** (tenant IDs, client IDs, scope) — embutidos no bundle da SPA em build-time por desenho; App Settings é o lugar certo, nada vai para GitHub/log/KV. Confirmado: o runbook (Bloco 4) e o AUTO-DECISION são explícitos e corretos. **Não-segredo confirmado.**
- **Guard extra** (L405-414): varredura do bundle contra API keys de LLM (`gsk_`/`AIza…`/`GEMINI_API_KEY`…) — camada defensiva bem-vinda.

#### 3. ux-review (formato aluno) — **PASS**
- **1 campo + 1 secret** materializados: `lab.config.json` = `{"sufixo": "suasiniciais"}`; único secret = `AZURE_CREDENTIALS`. Nenhuma GitHub Variable, nenhum publish profile, nenhum segredo de app digitado.
- **Fail-fast acionável:** guard do `sufixo` rejeita placeholder/vazio (L87) e valida charset do ACR (L91) com mensagens claras; o `jq … 2>/dev/null || true` (L85) garante que a mensagem amigável sempre saia mesmo com JSON malformado (fix F4). Fail-fast de identidade agora **simétrico** (5 `VITE_*`, lista todas as ausentes numa mensagem, aponta ao runbook — fix F2). 
- **README de staging conta a história completa:** mapa 15→1, convenção `<prefixo>-<sufixo>`, tabela do Doctor (7 itens), fluxo do aluno (AC-14), pendências. Coerente com o workflow e o runbook.

#### 4. Retro-compat (restrição dura do owner) — **PASS**
- `git status` = exatamente 3 arquivos no delta uncommitted (`lab-grande-final.yml`, `browser-harness-final-preprovisioning.md`, `4.5.story.md`). **Nenhum** workflow de lab existente tocado (`lab-oitavas-de-final.yml`/`lab-quartas-de-final.yml`/`lab-a-final.yml`/`deploy-phase-*.yml` intactos). O pacote vive em `docs/entrega/` (staging) + runbook — não roda do root (proteção intencional documentada no README). Honra AC-15. **Nada das Oitavas/Quartas quebra.**

#### Débitos aceitos (não-bloqueantes — glance antes de publicar o repo real)
- **D1 (nit, infra):** `KEYVAULT=kv-$SUFIXO` é derivado (L107) mas não referenciado diretamente no workflow — a validação KV/MI (item 7) resolve o cofre indiretamente via `keyVaultUrl` do McpServer. Env forward-looking/documental, inócuo. Manter ou remover é indiferente.
- **D2 (observação, Doctor item 6):** o check de perímetro é `[ -n "$EXTERNAL" ]` sobre `ingress.external` — passa se o ingress **existe** (external `true` OU `false`), não afirma `external==false`. É um smoke de presença, não uma asserção forte da invariante; a aplicação real da borda (ForwardLimit=1 + X-Gateway-Key) vive no código do gateway, validado no EPIC-003/4.2. Aceitável como pré-flight; se quiser endurecer no repo real, checar o valor esperado de `external` conforme a topologia alvo.

**Veredito:** os 4 findings do code review + O1 estão materialmente fechados e verificados; todas as 4 dimensões do gate passam; retro-compat intacta. **PASS.** Status da story **inalterado** (aguarda gate `@po` 6.1 + criação do repo real Task 1 — autorização do owner).

**Ação antes de publicar o repo real (`fifa2026-final-lab`):** (a) semear `lab.config.json` + `.github/workflows/lab-grande-final.yml` na **raiz** do repo (não em `docs/entrega/`) junto com o código-fonte da Final; (b) o instrutor provisionar os 11 recursos **estritamente** pela convenção `<prefixo>-<sufixo>` (senão `acao=check`/`tudo` não encontram os recursos) + semear as 5 `VITE_*` de identidade no App Settings do `app-frontend-<sufixo>` (runbook Bloco 4); (c) opcional: endurecer o Doctor item 6 (D2). Nenhum bloqueador de arquitetura/segurança.

---

### Gate `@po` (Pax) — Gate de UX do aluno — 2026-07-02

**Veredito: GO condicional (8/10).** A promessa central — 15 vars + 5 secrets → 1 campo (`sufixo`) + 1 secret (`AZURE_CREDENTIALS`) — está genuinamente entregue e rastreável (baseline confirmado em `quartas-f2-portal-guide.md` L272-301; colapso em `lab-grande-final.yml` L74-108/L353-393; único `secrets.*` = `AZURE_CREDENTIALS` L114). O fluxo "Use this template → editar sufixo → AZURE_CREDENTIALS → check → tudo" não pede nenhuma GitHub Variable nem segredo de app do aluno (AC-14 satisfeito). **Duas ressalvas MEDIUM antes de publicar o Template Repo para a turma:** (1) o Doctor `acao=check` NÃO verifica as 5 `VITE_*` de identidade que o AC-6/[AUTO-DECISION] tornou dependência do instrutor — um check "todos PASS" ainda pode quebrar no `acao=tudo`; (2) contradição de posse da KV-reference (runbook L31 "fica para a aula" ↔ Doctor item 7 exige pré-wired ↔ deploy não faz `secret set`), que gera FAIL falso no pré-flight de um ambiente montado conforme o runbook (overlap com o gate @architect). **LOW:** proveniência do `AZURE_CREDENTIALS` (é da turma, não criado pelo aluno) e a narrativa 15→1 merecem uma linha no README/portal-guide voltado ao aluno. AC-1/AC-2 (criação do repo real) seguem pendentes de autorização do owner — design completo. — Pax, equilibrando prioridades 🎯

#### Remediação das ressalvas do @po (2026-07-02, @dev Dex) — 2 MEDIUM + 2 LOW fechadas

- **MEDIUM #1 (Doctor não verifica as 5 `VITE_*`):** adicionado o **item 8** ao Doctor `acao=check` (`lab-grande-final.yml`) — `az webapp config appsettings list` no `app-frontend-<sufixo>` conferindo a **presença do nome** das 5 `VITE_*` de identidade (nunca valores), com FAIL apontando o instrutor + runbook Bloco 4. Contagem "7 checks → 8 checks" atualizada no workflow (comentário + nome do step), README (tabela do Doctor, +linha 8) e nas Completion Notes. _(A §Gate @architect acima cita "7 checks"/"7 itens" — é o registro histórico da auditoria dela, ANTERIOR ao item 8; preservado intacto por autoridade de agente. Item 8 é read-only, segue o padrão exato da leitura de App Settings já auditada e usada no bloco frontend, L356.)_
- **MEDIUM #2 (posse da KV-reference):** no runbook, o wiring das KV-references dos Container Apps foi **movido/declarado como pré-provisionamento** (Bloco 3, **passo 5** novo: os apps nascem lendo os app-secrets do cofre via MI, referenciando o `final-portal-guide.md` das 14 fases — **sem reabri-lo**) e o "fica para a aula" (L31) foi **reescopado** só para o que depende de FQDN (imagens de código + `VITE_*` de URL + CORS). Handoff e checkpoint atualizados. Agora **runbook ↔ Doctor item 7 contam a mesma história** — um ambiente montado conforme o runbook passa o item 7 (sem FALSO FAIL).
- **LOW #4 (proveniência do `AZURE_CREDENTIALS`):** linha voltada ao aluno no README de staging (passo 3 do fluxo) — "seu instrutor te entrega esse JSON; cole como está, NÃO crie um SP próprio".
- **LOW #5 (narrativa 15→1 no repo-raiz):** o plano de publicação (README) passou a incluir um **README-raiz voltado ao aluno**, com o texto pronto na nova seção "Conteúdo do README-raiz (aluno)" (tabela 15→1 resumida + "o que mudou desde as Quartas" + passo-a-passo só-web) para copiar na Task 1.

Retro-compat/invariantes preservadas (zero `az create` — item 8 é `list`/`--query`; único secret `AZURE_CREDENTIALS`); `final-portal-guide.md` **não** reaberto (apenas referenciado); zero segredo (item 8 e as `VITE_*` são identificadores OIDC públicos, checados por nome); YAML revalidado (js-yaml, 20 steps). Status **inalterado**.
